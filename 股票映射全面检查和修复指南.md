# 股票代码-名称映射全面检查和修复指南

## 📋 问题概述

您发现了**明星电力**的映射错误，这暴露了一个严重的数据质量问题。我们需要全面检查和修复所有类似的错误映射。

### 🔍 已发现的具体错误

1. **明星电力映射错误**
   - ❌ 错误：`000101` → "明星电力" 
   - ✅ 正确：`600101` → "明星电力"（上海主板）
   - ✅ 正确：`000101` → "恒邦股份"（深圳主板）

## 🛠️ 检查步骤

### 第一步：执行全面检查SQL

1. 打开 **pgAdmin** 或 **DataGrip**
2. 连接到 `stockdb` 数据库
3. 打开并执行文件：`db/find_all_wrong_mappings.sql`
4. 这个脚本会检查以下问题：

#### 检查项目清单

- [ ] **明星电力映射错误**（重点）
- [ ] **市场代码不匹配**（6开头应该是1，0/3开头应该是0）
- [ ] **重复名称**（同一名称对应多个代码）
- [ ] **市场名称错误**（上海/深圳标注错误）
- [ ] **名称缺失或不完整**（只有代码没有名称）
- [ ] **疑似非A股代码**（指数、基金、退市股等）
- [ ] **数据质量统计**（总体问题数量）

### 第二步：记录检查结果

执行SQL后，请记录以下信息：

1. **市场代码不匹配数量**：______ 条
2. **重复名称数量**：______ 组
3. **名称缺失数量**：______ 条
4. **疑似非A股数量**：______ 条

## 🔧 修复方案

### 方案A：立即修复明星电力（最紧急）

```sql
-- 在pgAdmin中执行
BEGIN;

-- 修正000101（深圳主板）
UPDATE stock_info 
SET stock_name = '恒邦股份',
    market_code = 0,
    market_name = '深圳',
    update_time = CURRENT_TIMESTAMP
WHERE stock_code = '000101';

-- 确认600101是明星电力（上海主板）
UPDATE stock_info 
SET stock_name = '明星电力',
    market_code = 1,
    market_name = '上海',
    update_time = CURRENT_TIMESTAMP
WHERE stock_code = '600101';

COMMIT;

-- 验证修复结果
SELECT stock_code, stock_name, market_code, market_name 
FROM stock_info 
WHERE stock_code IN ('000101', '600101');
```

### 方案B：批量修复市场代码

```sql
-- 批量修正所有市场代码不匹配的问题
BEGIN;

UPDATE stock_info
SET 
    market_code = CASE
        WHEN stock_code ~ '^6' THEN 1  -- 上海
        WHEN stock_code ~ '^0|^3' THEN 0  -- 深圳/创业板
        ELSE market_code
    END,
    market_name = CASE
        WHEN stock_code ~ '^6' THEN '上海'
        WHEN stock_code ~ '^0|^3' THEN '深圳'
        ELSE market_name
    END,
    update_time = CURRENT_TIMESTAMP
WHERE (
    (stock_code ~ '^6' AND market_code != 1) OR
    (stock_code ~ '^0|^3' AND market_code != 0)
);

COMMIT;

-- 验证结果
SELECT COUNT(*) as 修复数量 FROM stock_info
WHERE (
    (stock_code ~ '^6' AND market_code = 1) OR
    (stock_code ~ '^0|^3' AND market_code = 0)
);
```

### 方案C：停用非A股代码

```sql
-- 停用已知的非A股代码
BEGIN;

UPDATE stock_info
SET is_active = FALSE,
    update_time = CURRENT_TIMESTAMP
WHERE stock_code IN (
    '000046',  -- *ST泛海（已退市2024-02-07）
    '000139',  -- 富国国企债基金（非股票）
    '000914'   -- 300金融指数
)
AND is_active = TRUE;

COMMIT;

-- 验证结果
SELECT stock_code, stock_name, is_active 
FROM stock_info 
WHERE stock_code IN ('000046', '000139', '000914');
```

## 📊 修复后验证

### 验证脚本

```sql
-- 1. 验证明星电力已修正
SELECT '明星电力验证' as 项目,
       stock_code, stock_name, market_code
FROM stock_info
WHERE stock_code IN ('000101', '600101');

-- 2. 验证市场代码全部正确
SELECT '市场代码错误数' as 项目,
       COUNT(*) as 数量
FROM stock_info
WHERE is_active = TRUE
  AND (
    (stock_code ~ '^6' AND market_code != 1) OR
    (stock_code ~ '^0|^3' AND market_code != 0)
  );

-- 3. 验证非A股已停用
SELECT '非A股停用验证' as 项目,
       stock_code, stock_name, is_active
FROM stock_info
WHERE stock_code IN ('000046', '000139', '000914');
```

预期结果：
- ✅ `000101` = 恒邦股份（深圳，市场代码0）
- ✅ `600101` = 明星电力（上海，市场代码1）
- ✅ 市场代码错误数 = 0
- ✅ 所有非A股代码 `is_active = FALSE`

## 🔄 同步到应用程序

修复数据库后，需要同步到应用程序的内置字典：

### 1. 修改 `StockInfoCache.cs`

找到文件：`src/Core/Cache/StockInfoCache.cs`

#### 修改点1：修正 builtinNames 字典

```csharp
// 修复前（第130行左右）
{"000101", "明星电力"},  // ❌ 错误！

// 修复后
{"000101", "恒邦股份"},  // ✅ 正确！深圳主板
```

#### 修改点2：确保600101在字典中

```csharp
// 如果没有，添加这行
{"600101", "明星电力"},  // ✅ 上海主板
```

### 2. 重新编译和部署

```batch
cd F:\dsfr\mqq
msbuild MQReceiver.sln /p:Configuration=Release /t:Rebuild
```

### 3. 重启应用程序

```batch
taskkill /F /IM MQReceiver.exe
cd bin\Release
MQReceiver.exe
```

## 📝 需要手动确认的股票代码

以下代码需要您联网查询确认是否为A股/创业板：

| 代码 | 当前名称 | 需确认 |
|------|---------|--------|
| 000005 | ST星源 | 是否A股？ |
| 000038 | （查询） | 是否A股？ |
| 000076 | （查询） | 是否A股？ |
| 000110 | （查询） | 是否A股？ |
| 000974 | （查询） | 是否A股？ |
| 000992 | （查询） | 是否A股？ |

### 确认方法

1. 访问东方财富网：`http://quote.eastmoney.com/sz{代码}.html`
2. 或新浪财经：`https://finance.sina.com.cn/realstock/company/sz{代码}/nc.shtml`
3. 确认是否为A股主板或创业板股票
4. 如果不是，则添加到黑名单

## 🎯 预期效果

修复完成后，应用程序应该：

1. ✅ **正确显示明星电力（600101）**
2. ✅ **000101显示为恒邦股份**
3. ✅ **所有市场代码正确**
4. ✅ **非A股不再出现在过滤结果中**
5. ✅ **所有股票名称显示完整**

## ⚠️ 注意事项

1. **备份数据库**：修复前请先备份 `stock_info` 表
   ```sql
   CREATE TABLE stock_info_backup_20260120 AS 
   SELECT * FROM stock_info;
   ```

2. **分步执行**：建议按照修复方案A → B → C的顺序执行，每步验证后再继续

3. **测试验证**：修复后启动应用程序，执行一次过滤，检查结果是否正确

4. **日志监控**：观察控制台日志，确认没有"股票名称缺失"的警告

## 📞 支持

如果遇到问题，请提供：
1. 检查SQL的执行结果截图
2. 修复SQL的执行结果
3. 应用程序控制台的错误日志
4. 具体哪些股票代码仍然显示不正确

---

**生成时间**：2026-01-20  
**版本**：v1.0  
**状态**：等待用户执行检查SQL
