# 盘中卡顿优化说明

## 问题分析

### 核心问题
盘中有大量频繁的实时数据更新和重复计算，而盘后没有实时数据更新，只使用静态的历史数据。

### 主要卡顿原因

1. **实时数据更新触发频繁重新计算**
   - RealTimeDataCache 每次接收到MQ推送时都会立即触发 `DataUpdated` 事件
   - 盘中每次实时数据更新后，ChartService 的缓存会因为TTL很短(盘中10-20秒)而快速失效

2. **WebChartWindow 频繁重新加载图表数据**
   - 盘中图表缓存TTL只有 20秒，盘外60秒
   - 拖动K线图时频繁触发 `Window_SizeChanged` 事件，进而调用JavaScript重绘
   - 实时数据合并需要计算前复权价格

3. **KD指标重复计算**
   - `CalculateKDWithSmoothing` 需要聚合周期K线、计算RSV/K/D值、线性插值平滑处理
   - 周/月/季KD并行计算，但盘中数据频繁变化导致缓存失效，需要重新计算

4. **JSON序列化开销**
   - `JsonConvert.SerializeObject(chartData)` 需要序列化完整的图表数据
   - 虽然有缓存优化，但盘中20秒TTL导致经常需要重新序列化

### 性能瓶颈对比表

| 操作 | 盘中频率 | 盘后频率 | 性能影响 |
|------|---------|---------|---------|
| 实时数据更新 | 每秒多次 | 0 | ⭐⭐⭐⭐⭐ |
| 图表数据缓存失效 | 每20秒 | 每60秒 | ⭐⭐⭐⭐ |
| KD缓存失效 | 每10秒 | 每30秒 | ⭐⭐⭐⭐ |
| 前复权价格计算 | 频繁 | 极少 | ⭐⭐⭐ |
| JSON序列化 | 频繁 | 极少 | ⭐⭐⭐ |

## 优化措施

### 优化1：延长盘中缓存TTL

**修改文件：**
- `src/UI/Views/WebChartWindow.xaml.cs`
- `src/UI/ChartService.cs`

**改动详情：**

#### WebChartWindow.xaml.cs
```csharp
// 修改前
private const int ChartCacheTTLSecondsTrading = 20;   // 盘中 20 秒
private const int ChartCacheTTLSecondsNonTrading = 60; // 盘外 60 秒

// 修改后
private const int ChartCacheTTLSecondsTrading = 120;   // 盘中 120 秒
private const int ChartCacheTTLSecondsNonTrading = 180; // 盘外 180 秒
```

#### ChartService.cs
```csharp
// 修改前
private const int DefaultKlineCacheTTLSeconds = 30;
private const int DefaultKlineCacheTTLTradingSeconds = 10;

// 修改后
private const int DefaultKlineCacheTTLSeconds = 60;        // 盘外 60秒
private const int DefaultKlineCacheTTLTradingSeconds = 30; // 盘中 30秒
```

**预期效果：**
- 盘中图表数据重新加载频率从每20秒降低到每120秒（6倍减少）
- KD缓存重新计算频率从每10秒降低到每30秒（3倍减少）
- 显著降低CPU和内存使用率

---

### 优化2：防抖实时数据更新

**修改文件：**
- `src/Core/Cache/RealTimeDataCache.cs`

**核心改动：**

添加防抖定时器，将2秒内的多次实时数据更新聚合为一次事件触发：

```csharp
// 新增字段
private readonly System.Timers.Timer _debounceTimer;
private readonly List<string> _pendingUpdatedStockCodes = new List<string>();
private const int DebounceIntervalMs = 2000; // 2秒防抖

// 修改 UpdateData 方法
public void UpdateData(List<RealTimeDataRecord> records)
{
    // ... 更新缓存 ...

    // 优化2：将更新的股票代码添加到待处理列表
    lock (_debounceLock)
    {
        _pendingUpdatedStockCodes.AddRange(updatedStockCodes);
    }

    // 重启防抖定时器（2秒内的多次更新会被聚合）
    _debounceTimer?.Stop();
    _debounceTimer?.Start();
}

// 定时器触发时批量触发事件
private void OnDebounceTimerElapsed(object sender, ElapsedEventArgs e)
{
    List<string> updatedCodes;
    lock (_debounceLock)
    {
        updatedCodes = new List<string>(_pendingUpdatedStockCodes);
        _pendingUpdatedStockCodes.Clear();
    }

    if (updatedCodes.Count > 0)
    {
        DataUpdated?.Invoke(this, updatedCodes);
    }
}
```

**工作原理：**
1. MQ服务推送实时数据时，数据先写入缓存，但不立即触发事件
2. 启动2秒防抖定时器
3. 2秒内的所有更新都会被聚合到 `_pendingUpdatedStockCodes` 列表
4. 2秒后定时器触发，一次性批量触发 `DataUpdated` 事件

**预期效果：**
- 盘中每秒多次的事件触发降低到每2秒一次（10-100倍减少）
- 大幅降低UI刷新频率和计算触发频率
- 数据仍然实时更新到缓存，只是事件通知被延迟和聚合

---

### 优化3：窗口拖动防抖

**修改文件：**
- `src/UI/Views/WebChartWindow.xaml.cs`

**核心改动：**

添加resize防抖定时器，避免拖动窗口时频繁触发JavaScript重绘：

```csharp
// 新增字段
private DispatcherTimer _resizeDebounceTimer;
private bool _isResizing = false;

// 初始化方法中添加
private void InitializeBoundsPersistence()
{
    // ... 原有代码 ...

    // 初始化resize防抖定时器（延迟500ms）
    _resizeDebounceTimer = new DispatcherTimer { Interval = TimeSpan.FromMilliseconds(500) };
    _resizeDebounceTimer.Tick += async (s, ev) =>
    {
        _resizeDebounceTimer.Stop();
        _isResizing = false;

        // 执行实际的resize操作
        if (_isWebViewInitialized && webView?.CoreWebView2 != null)
        {
            try
            {
                await webView.ExecuteScriptAsync("window.dispatchEvent(new Event('resize'));");
            }
            catch (Exception) { }
        }
    };
}

// 修改 Window_SizeChanged
private async void Window_SizeChanged(object sender, SizeChangedEventArgs e)
{
    ScheduleSaveWindowBounds();

    // 使用防抖机制，避免拖动时频繁触发resize
    _isResizing = true;
    _resizeDebounceTimer?.Stop();
    _resizeDebounceTimer?.Start();
}
```

**工作原理：**
1. 窗口大小改变时，先标记为正在resize
2. 启动500ms防抖定时器
3. 500ms内的多次resize事件会被忽略
4. 500ms后才执行一次JavaScript的resize重绘

**预期效果：**
- 拖动窗口时从每帧触发resize降低到停止拖动后500ms触发一次
- 大幅减少WebView2的JavaScript执行和图表重绘次数
- 拖动更流畅，不再卡顿

---

## 优化效果预估

### 盘中性能提升

| 优化项 | 优化前 | 优化后 | 提升倍数 |
|-------|--------|--------|---------|
| 图表数据重新加载 | 每20秒 | 每120秒 | 6倍 |
| KD缓存重新计算 | 每10秒 | 每30秒 | 3倍 |
| DataUpdated事件触发 | 每秒多次 | 每2秒一次 | 10-100倍 |
| 窗口resize重绘 | 每帧 | 停止后500ms一次 | 60倍+ |

### 综合性能提升

- **CPU使用率**：预计降低 50-70%
- **UI响应速度**：预计提升 3-5倍
- **拖动流畅度**：预计提升 10倍以上
- **内存占用**：基本不变或略有降低

### 注意事项

1. **数据实时性权衡**
   - 优化后数据仍然实时更新到缓存
   - 但UI刷新和事件触发有2秒延迟
   - 对于股票分析场景，2秒延迟完全可接受

2. **缓存一致性**
   - 延长TTL后，盘中数据更新到UI的延迟会略有增加
   - 但由于有防抖机制，实际数据已经更新到缓存，只是计算触发延迟

3. **可配置性**
   - 如果需要更实时的更新，可以调整 `DebounceIntervalMs` (默认2000ms)
   - 如果需要更激进的缓存，可以进一步延长 TTL

## 后续优化建议

1. **延迟加载实时数据**
   - 图表初次打开时使用历史数据
   - 之后才定期更新实时数据
   - 添加"暂停实时更新"按钮

2. **减少前复权计算**
   - 缓存前复权系数，而不是每次都重新计算
   - 只在除权除息日更新系数

3. **智能缓存失效**
   - 根据数据变化情况动态调整TTL
   - 盘中无数据更新时自动延长TTL

4. **WebView2渲染优化**
   - 使用虚拟滚动减少DOM节点数量
   - 使用Canvas替代SVG渲染大量K线数据

## 测试建议

1. **功能测试**
   - 验证盘中数据仍然能正常更新
   - 验证图表拖动和缩放正常工作
   - 验证K线图显示正确

2. **性能测试**
   - 使用任务管理器监控CPU和内存使用
   - 盘中打开多个图表窗口测试流畅度
   - 对比优化前后的拖动体验

3. **边界测试**
   - 测试盘前、盘中、盘后切换
   - 测试数据大量更新时的表现
   - 测试长时间运行的稳定性

## 总结

本次优化主要针对盘中卡顿问题，通过以下三项措施：

1. **延长缓存TTL** - 减少重复计算
2. **防抖数据更新** - 减少事件触发频率
3. **窗口拖动防抖** - 减少重绘次数

预计可以将盘中性能提升 **3-10倍**，显著改善用户体验，同时保持数据的实时性和准确性。
