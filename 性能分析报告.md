# MQReceiver 性能分析报告

## 一、已做的优化（当前状态）

| 模块 | 优化点 | 说明 |
|------|--------|------|
| **过滤/KD** | 金额先过滤再算 KD | 不达标股票直接跳过，减少无效 KD 计算 |
| **过滤/KD** | 批量查成交金额 | `GetYesterdayAmountAndTurnoverRateBatch` 一次查全量，避免 N 次查询 |
| **过滤/KD** | 6 条件并行度限制 | 每条件 `(ProcessorCount*2)/6`，避免 6×32 总并发导致 OOM |
| **过滤/KD** | 目标日期一致 | 批量预计算与 6 个计算器用同一 `targetDate`，避免缓存未命中 |
| **ChartService** | K 线/KD 缓存 TTL | 盘中 10 秒、盘外 30 秒，可配置 |
| **ChartService** | K 线/KD 缓存上限 | 超 400 条按 LRU 淘汰，防无限增长 |
| **ChartService** | 打开图表单次加载 | `days==0` 时 `LoadDailyKlineDataAndRealOnce` 只拉一次日线，前复权+未复权一起算 |
| **UI 涨幅** | 节流 100ms | `OnRealTimeDataUpdated` 合并 100ms 内更新再刷新 |
| **UI 涨幅** | 批量查实时数据 | `GetDataBatch(stockCodes)` 一次取多只股票 |
| **K 线仓库** | 内存缓存 + 过期 | `PostgresKlineDataRepository` 按股票缓存，过期清理 |
| **进程** | 64 位 | `Prefer32Bit=false`，突破 2GB 限制 |

---

## 二、仍可优化的热点

### 1. 日线保存：逐行 ExecuteNonQuery（高价值）

**位置**：`PostgresStockDataRepository.SaveDailyData`

**现状**：每 500 条一批事务，但**每条记录一次 `new NpgsqlCommand` + `ExecuteNonQuery()`**，即每批 500 次网络往返。

**建议**：
- 使用 **Npgsql 批量写入**：同一批用一条带多组参数的 `INSERT ... VALUES (...),(...),...`，或 `NpgsqlBinaryImporter`（COPY），将 500 条合并为 1 次往返。
- 预期：MQ 接收大批日线时，写入耗时可明显下降（约一个数量级）。

---

### 2. 表格刷新：ObservableCollection 逐条 Add（中价值）

**位置**：`FilterMainViewModel.UpdateTableResults`、`SortByPriceChange`

**现状**：`targetResults.Clear()` 后 `foreach (var item in newItems) { targetResults.Add(item); }`，每次 `Add` 触发 `CollectionChanged`，8 个表 × 每表 N 条 ≈ 8N 次通知，DataGrid 可能重绘多次。

**建议**：
- 若 WPF 版本/第三方库支持：使用「批量更新」或「暂停通知」的集合，先 Clear + Add 完再一次性通知。
- 或：用新列表替换 `ItemsSource`（例如 `targetResults = new ObservableCollection<...>(newItems)` 再 `OnPropertyChanged`），减少中间状态的重绘（需确认绑定方式是否支持整体替换）。

---

### 3. StockInfoCache 启动与重复同步（中价值）

**位置**：`FilterMainWindow.InitializeStockInfoCache`、`FilterService` 内 `StockInfoCache.EnsureLoaded()` / `SyncFromDailyData()`

**现状**：主窗口打开时同步一次；每次计算前 `EnsureLoaded()`，若为空再 `SyncFromDailyData()`，可能重复做较重同步。

**建议**：
- 增加「已完整同步」标记（例如某次成功 `SyncFromDailyData` 后置位），在 T 分钟内或当次进程内不再做完整同步，只做轻量 `EnsureLoaded()`（读内存）。
- 或将主窗口的 `InitializeStockInfoCache` 改为后台异步，避免首屏卡顿。

---

### 4. 进度日志频率（低价值）

**位置**：`UnifiedStockFilter.FilterParallel` 内 `processedCount % 100 == 0` 且 `processedCount % 500 == 0` 时打日志。

**现状**：每 500 条打一次进度，高并行时仍会有一定控制台/日志 I/O。

**建议**：可配置化（如仅当 `FilterVerboseLog` 为 true 时输出），或改为每 1000/2000 条输出一次，减少 I/O。

---

### 5. 实时数据里的 GetStockName（低价值）

**位置**：`MQServiceWrapper.ProcessRealTimeData` 中，对每条 `record` 若 `StockName` 为空则 `StockInfoCache.Instance.GetStockName(record.StockCode)`。

**现状**：`StockInfoCache` 为内存字典，单次查找成本低；仅在新推的股票较多且都缺名称时会有 N 次调用。

**建议**：若推送批次很大且缺名称的很多，可先收集缺名称的 `StockCode`，再批量查一次（若后续提供 `GetStockNames(IEnumerable<string> codes)` 再替换）；否则可保持现状。

---

## 三、数据库与运行环境建议

- **PostgreSQL**：`stock_daily_data` 上若有按 `(stock_code, trade_date)` 的查询，建议保留或创建对应唯一索引/主键，便于 ON CONFLICT 与按股票+日期范围的查询。
- **连接池**：已使用 Npgsql 连接池，保持现有配置即可；若 MQ 写入与过滤并发高，可适当调大 `DatabaseMaxPoolSize`（需与库最大连接数匹配）。
- **内存**：批量 KD + 全量 K 线预加载会占用较多内存，已通过 64 位与缓存条数上限控制；若仍 OOM，可考虑关闭「批量计算器」或减少预加载范围。

---

## 四、优化优先级建议

| 优先级 | 项目 | 预期收益 | 改动量 |
|--------|------|----------|--------|
| 高 | 日线保存改为批量 INSERT/COPY | MQ 接收写入明显加快 | 中 |
| 中 | 表格刷新改为批量/整体替换 | 计算结束后界面更流畅 | 中 |
| 中 | StockInfoCache 防重复同步 + 异步初始化 | 启动/计算前等待更短 | 小 |
| 低 | 进度日志可配置或降频 | 控制台更安静、略减 I/O | 小 |
| 低 | 实时数据缺名称时批量查名 | 大批量推送时略减开销 | 小 |

若你希望先做其中某一项，我可以按该优先级给出具体改法（含文件与接口级别说明）。
