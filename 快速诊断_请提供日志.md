# 快速诊断脚本 - 检查为什么过滤不出数据

请按照以下步骤操作：

## 步骤1：运行程序并查看日志

运行程序后，在控制台查找以下关键信息：

### A. 成交金额查询（在批量KD计算之前）
```
[性能优化] 批量查询成交金额和换手率: XXXX只股票, 查询到: YYYY条数据
```
**把这行完整复制给我！**

### B. 批量KD计算结果
```
[批量KD计算] 预计算完成！
  成功: XXXX 只
  数据不足: XXXX 只
  失败: XXXX 只
```
**把这几行完整复制给我！**

### C. 条件5的诊断信息（在"【强多反弹】完成"之后）
```
[表格5/6诊断] 在「有今日+昨日KD 且 昨天成交金额>=N亿」的股票中，各子条件满足数：
  候选数(有KD且金额达标): XXXX
  条件5(强多反弹): A2>=M1:XXXX  K1<M3:XXXX  K1<A2:XXXX  全部:XXXX
```
**把这几行完整复制给我！**

### D. 目标日期（在"开始并行执行6个计算条件"之前）
```
[目标日期] 2026-01-XX（批量预计算与 6 个条件均使用此日期）
```
**把这行复制给我！**

---

## 步骤2：如果找不到日志，执行SQL检查

### 检查1：数据库中有没有成交金额数据
```sql
SELECT trade_date, COUNT(*) as total,
       COUNT(amount) as has_amount,
       COUNT(CASE WHEN amount >= 500000000 THEN 1 END) as amount_gt_5yi
FROM daily_data
WHERE trade_date >= CURRENT_DATE - INTERVAL '10 days'
GROUP BY trade_date
ORDER BY trade_date DESC;
```

**预期结果：**
- has_amount 应该 > 0
- amount_gt_5yi 应该 > 0（有成交额超过5亿的股票）

### 检查2：KD数据是否存在
```sql
SELECT COUNT(DISTINCT stock_code) as stocks_with_kd
FROM kd_values
WHERE trade_date >= CURRENT_DATE - INTERVAL '5 days';
```

**预期结果：**
- stocks_with_kd 应该 > 1000

### 检查3：查看某只股票的完整数据
```sql
-- 以平安银行(000001)为例
SELECT dd.trade_date, dd.close, dd.amount, dd.turnover_rate,
       kd.weekly_k, kd.monthly_k, kd.quarterly_k
FROM daily_data dd
LEFT JOIN kd_values kd ON dd.stock_code = kd.stock_code AND dd.trade_date = kd.trade_date
WHERE dd.stock_code = '000001'
ORDER BY dd.trade_date DESC
LIMIT 5;
```

**预期结果：**
- amount 不应该为 null
- weekly_k, monthly_k, quarterly_k 应该有值

---

## 步骤3：临时快速测试

如果你等不及诊断，想快速验证系统是否工作，可以：

### 方法A：修改配置文件（大幅放宽条件）

找到配置文件（通常是 `config.ini` 或 `appsettings.json`），修改：

```ini
# 临时测试配置 - 极度放宽条件
GlobalThreshold_M1 = 30   # 原值78，降到30
GlobalThreshold_M2 = 20   # 原值65，降到20
GlobalThreshold_M3 = 10   # 原值50，降到10
GlobalThreshold_M4 = 5    # 原值30，降到5
GlobalThreshold_N = 0.1   # 原值5（亿），降到0.1亿（1千万）
PriceChangeFilterThreshold = 50  # 原值7%，增加到50%
```

重新运行，看是否有结果。

**如果仍然 = 0** → 数据问题（KD数据或成交金额数据缺失）
**如果有结果** → 原阈值设置过严格

---

## 步骤4：最快的诊断方法

**直接告诉我这3个数字：**

运行程序后，从控制台输出中找到：

1. **批量查询成交金额** 查询到了 `___` 条数据
2. **批量KD计算** 成功了 `___` 只股票
3. **候选数(有KD且金额达标)** = `___`

只要这3个数字，我就能马上告诉你问题在哪里！

---

## 我需要的信息（按优先级）

### 🔴 最重要（必须提供）
```
1. [性能优化] 批量查询成交金额和换手率: ___只股票, 查询到: ___条数据
2. [批量KD计算] 成功: ___ 只, 数据不足: ___ 只
3. [表格5/6诊断] 候选数(有KD且金额达标): ___
```

### 🟡 很重要（最好提供）
```
4. [目标日期] 2026-01-__
5. [表格5/6诊断] 条件5: A2>=M1:___ K1<M3:___ K1<A2:___ 全部:___
```

### 🟢 可选（帮助进一步诊断）
```
6. 完整的诊断日志输出
7. SQL查询结果
```

---

**请提供以上信息，我会立即帮你定位问题！**
