# KD指标计算公式验证

## 标准KD指标公式

### 1. RSV（Raw Stochastic Value）计算
```
RSV = (收盘价 - 最低价) / (最高价 - 最低价) * 100
```
- **收盘价**：当前周期的收盘价
- **最高价**：过去N个周期内的最高价
- **最低价**：过去N个周期内的最低价
- **N**：默认周期为9（可配置）

### 2. K值计算
```
K = (2/3) * 前一日K值 + (1/3) * 当日RSV
```
- **初始K值**：50
- **平滑系数**：2/3（前一日权重）+ 1/3（当日RSV权重）

### 3. D值计算
```
D = (2/3) * 前一日D值 + (1/3) * 当日K值
```
- **初始D值**：50
- **平滑系数**：2/3（前一日权重）+ 1/3（当日K值权重）

## 代码实现验证

### 当前实现位置
- 文件：`src/DataProcessing/Calculators/KDCalculator.cs`
- 方法：`CalculateKD`（第212-314行）

### 公式实现检查

#### ✅ RSV计算（第280行）
```csharp
decimal rsv = (close - lowest) / (highest - lowest) * 100;
```
- ✅ 公式正确
- ✅ 处理了除零情况（highest == lowest 时返回50）

#### ✅ K值计算（第291行）
```csharp
k = (2m / 3m) * k + (1m / 3m) * rsv;
```
- ✅ 公式正确
- ✅ 使用decimal类型保证精度
- ✅ 初始值50正确

#### ✅ D值计算（第292行）
```csharp
d = (2m / 3m) * d + (1m / 3m) * k;
```
- ✅ 公式正确
- ✅ 使用decimal类型保证精度
- ✅ 初始值50正确

### 数据聚合逻辑

#### 周/月/季KD的特殊处理
对于周KD、月KD、季KD，代码先按周期聚合日线数据：
- **周KD**：按周分组（周一到周日）
- **月KD**：按月分组
- **季KD**：按季度分组

然后在聚合后的数据上计算KD指标，这是**正确的做法**。

#### 聚合后的K线数据
```csharp
aggregated.Add(new AggregatedCandle
{
    Date = group.Last().Date,      // 使用最后一天的日期
    Open = group.First().Open,     // 开盘价取第一天的
    High = group.Max(d => d.High), // 最高价取最大值
    Low = group.Min(d => d.Low),   // 最低价取最小值
    Close = group.Last().Close,    // 收盘价取最后一天的
    Volume = group.Sum(d => d.Volume) // 成交量求和
});
```
- ✅ 聚合逻辑正确

### RSV计算中的周期选择

在计算RSV时，代码使用：
```csharp
var periodData = aggregatedData.Skip(i - actualPeriod + 1).Take(actualPeriod).ToList();
decimal highest = periodData.Max(data => data.High);
decimal lowest = periodData.Min(data => data.Low);
decimal close = aggregatedData[i].Close;
```

**说明**：
- `periodData`：包含当前周期及前N-1个周期的数据（共N个周期）
- `highest`：这N个周期内的最高价
- `lowest`：这N个周期内的最低价
- `close`：当前周期的收盘价

这是**正确的实现**，符合KD指标的标准算法。

## 调试输出

已添加详细的调试输出，包括：
1. **RSV计算详情**：显示前5个周期的RSV计算过程
2. **K和D值计算过程**：显示前5个周期的K和D值递推计算
3. **最终结果**：显示计算出的K、D、RSV和K-D差值

### 如何查看调试信息

运行程序时，对于股票代码 `000001`，会在控制台输出：
```
[KD调试] 000001 week周期 (period=9):
  目标日期: 2025-01-26
  聚合后数据量: 150
  聚合数据首条: 2020-01-01
  聚合数据末条: 2025-01-26
  RSV计算完成，共 142 个值
    RSV[0]: 日期=2020-03-01, 收盘=10.50, 最高=11.20, 最低=9.80, RSV=45.50
    ...
  K和D值计算过程（前5个周期）:
    周期[0]: RSV=45.50, K=50.00 -> 48.50, D=50.00 -> 49.50
    ...
  最终结果: K=65.30, D=62.10, RSV=58.20, K-D=3.20
```

## 潜在问题排查

### 1. 数据不足
如果聚合后的数据量少于周期数（默认9），KD计算可能不准确。
- **检查**：查看 `[KD调试]` 输出中的"聚合后数据量"
- **解决**：确保从股票的最早数据开始计算

### 2. 数据范围问题
如果目标日期超出股票数据范围，会使用股票的最新可用日期。
- **检查**：查看 `[KD调试]` 输出中的"目标日期"和"聚合数据末条"
- **解决**：确保数据范围正确

### 3. 实时数据合并
如果启用了实时数据合并，需要确保实时数据正确合并到历史数据中。
- **检查**：查看 `GetAggregatedData` 方法中的实时数据合并逻辑
- **解决**：确保实时数据的日期和价格正确

## 结论

✅ **KD计算公式实现正确**
- RSV计算公式正确
- K值计算公式正确
- D值计算公式正确
- 数据聚合逻辑正确
- 周期选择逻辑正确

如果KD差值图没有显示数据，可能的原因：
1. **数据传递问题**：K和D值没有正确传递到前端
2. **前端计算问题**：K-D差值计算或图表设置有问题
3. **数据量问题**：KD数据量太少，无法显示

建议：
1. 查看C#控制台的 `[KD调试]` 输出，确认KD值是否正确计算
2. 查看浏览器控制台的 `setKDBandData` 和 `calculateKDDiff` 输出，确认数据是否正确传递和计算
3. 检查图表系列是否正确初始化
