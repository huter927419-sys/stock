# 6个过滤条件更新说明

## 📋 **更新概述**

已将原8个过滤条件更新为6个新的过滤条件，参数和逻辑按用户要求重新定义。

---

## 🎯 **参数定义**

| 参数 | 默认值 | 说明 |
|------|--------|------|
| M1 | 78 | 强多阈值 |
| M2 | 65 | 中多阈值 |
| M3 | 50 | 缠绕/反弹阈值 |
| M4 | 30 | 中多反弹阈值 |

---

## 🔍 **6个过滤条件详解**

### **条件1：强多排列**

**公式**：`K1 > K2 AND K1 > K3 AND K3 >= M1`

**说明**：
- 周K值最大（强势）
- 季K值 ≥ 78（强多区域）
- 三个K值呈下降排列

**技术含义**：最强的多头排列，短期最强，长期也强。

---

### **条件2：中多排列**

**公式**：`K1 > K2 AND K1 > K3 AND K3 >= M2 AND K3 < M1`

**说明**：
- 周K值最大（强势）
- 季K值在65-78之间（中多区域）
- 三个K值呈下降排列

**技术含义**：中等强度的多头排列，短期强，长期中等。

---

### **条件3：强多缠绕**

**公式**：`((K1 < K2 AND K2 > K3) OR (K1 < K3 AND K3 > K2)) AND K3 >= M1 AND K1 > M3`

**说明**：
- K值出现缠绕（K2最大或K3最大）
- 季K值 ≥ 78（强多区域）
- 周K值 > 50（避免弱势）

**技术含义**：
- 缠绕状态：中期形成顶部，但整体仍在强多区域
- 可能出现回调后再突破

---

### **条件4：中多缠绕**

**公式**：`((K1 < K2 AND K2 > K3) OR (K1 < K3 AND K3 > K2)) AND K3 >= M2 AND K3 < M1 AND K1 > M3`

**说明**：
- K值出现缠绕（K2最大或K3最大）
- 季K值在65-78之间（中多区域）
- 周K值 > 50（避免弱势）

**技术含义**：中等强度的缠绕状态，可能蓄势待发。

---

### **条件5：强多反弹**

**公式**：`K2 >= M1 AND K3 >= M1 AND K1 < M3 AND K1 >= REF(K1,1)`

**说明**：
- 月K值 ≥ 78 且 季K值 ≥ 78（长期强多）
- 周K值 < 50（短期回调）
- 周K值开始回升（K1 ≥ 昨日K1）

**技术含义**：
- 长期趋势强劲，短期出现回调
- 周K值开始反弹，可能是买入机会

---

### **条件6：中多反弹**

**公式**：`K2 >= M2 AND K3 >= M2 AND (K2 < M1 OR K3 < M1) AND K1 < M4 AND K1 >= REF(K1,1)`

**说明**：
- 月K值 ≥ 65 且 季K值 ≥ 65（中期强势）
- 但至少一个 < 78（不是强多）
- 周K值 < 30（深度回调）
- 周K值开始回升（K1 ≥ 昨日K1）

**技术含义**：中等强度的反弹，回调较深，反弹力度可能更大。

---

## 📊 **修改文件清单**

### 1️⃣ **src/Filters/NewFilterConditions.cs**

**添加M4参数**：
```csharp
public decimal M4 { get; set; }

public NewFilterCondition(int filterId)
{
    // ...
    M4 = config.GetDecimal("GlobalThreshold_M4", 30m);
}
```

**更新CheckCondition方法**：
```csharp
public bool CheckCondition(decimal k1, decimal k2, decimal k3, decimal refK1, decimal refK2)
{
    switch (FilterId)
    {
        case 1: // 强多排列
            return k1 > k2 && k1 > k3 && k3 >= M1;
        
        case 2: // 中多排列
            return k1 > k2 && k1 > k3 && k3 >= M2 && k3 < M1;
        
        case 3: // 强多缠绕
            return ((k1 < k2 && k2 > k3) || (k1 < k3 && k3 > k2)) 
                   && k3 >= M1 && k1 > M3;
        
        case 4: // 中多缠绕
            return ((k1 < k2 && k2 > k3) || (k1 < k3 && k3 > k2)) 
                   && k3 >= M2 && k3 < M1 && k1 > M3;
        
        case 5: // 强多反弹
            return k2 >= M1 && k3 >= M1 && k1 < M3 && k1 >= refK1;
        
        case 6: // 中多反弹
            return k2 >= M2 && k3 >= M2 && (k2 < M1 || k3 < M1) 
                   && k1 < M4 && k1 >= refK1;
        
        default:
            return false;
    }
}
```

---

### 2️⃣ **src/Filters/FilterService.cs**

**从8个过滤器改为6个**：
```csharp
// 并行执行6个过滤器
var filter1Task = Task.Run(() => ExecuteNewFilter(1, "强多排列", targetDate));
var filter2Task = Task.Run(() => ExecuteNewFilter(2, "中多排列", targetDate));
var filter3Task = Task.Run(() => ExecuteNewFilter(3, "强多缠绕", targetDate));
var filter4Task = Task.Run(() => ExecuteNewFilter(4, "中多缠绕", targetDate));
var filter5Task = Task.Run(() => ExecuteNewFilter(5, "强多反弹", targetDate));
var filter6Task = Task.Run(() => ExecuteNewFilter(6, "中多反弹", targetDate));

Task.WaitAll(filter1Task, filter2Task, filter3Task, filter4Task, filter5Task, filter6Task);
```

**更新结果处理**：
```csharp
OnFilterCompleted(new FilterResultEventArgs
{
    Table1Results = enrichedResults1,
    Table2Results = enrichedResults2,
    Table3Results = enrichedResults3,
    Table4Results = enrichedResults4,
    Table5Results = enrichedResults5,
    Table6Results = enrichedResults6,
    Table7Results = new List<FilterResultWithHistory>(), // 空列表
    Table8Results = new List<FilterResultWithHistory>(), // 空列表
    // ...
});
```

---

### 3️⃣ **src/Filters/UnifiedStockFilter.cs**

**更新注释**：
```csharp
/// <summary>
/// 统一股票过滤器 - 用于新的6个过滤条件
/// 包含涨幅计算功能
/// </summary>
```

---

## 🚀 **使用方法**

### **默认参数（推荐）**

程序会自动使用默认参数：
- M1 = 78
- M2 = 65
- M3 = 50
- M4 = 30

### **自定义参数**

在 `App.config` 中添加配置：
```xml
<appSettings>
    <add key="GlobalThreshold_M1" value="78" />
    <add key="GlobalThreshold_M2" value="65" />
    <add key="GlobalThreshold_M3" value="50" />
    <add key="GlobalThreshold_M4" value="30" />
</appSettings>
```

---

## 📈 **预期效果**

### **过滤结果分布**

| 过滤条件 | 预期股票数 | 特征 |
|---------|-----------|------|
| 1. 强多排列 | 少（10-30只） | 最强势，风险最低 |
| 2. 中多排列 | 中等（30-80只） | 强势，风险较低 |
| 3. 强多缠绕 | 少（10-40只） | 强势缠绕，可能突破 |
| 4. 中多缠绕 | 中等（30-100只） | 中等缠绕，蓄势 |
| 5. 强多反弹 | 少（5-20只） | 强势回调，买入机会 |
| 6. 中多反弹 | 中等（20-60只） | 深度回调，反弹空间大 |

---

## 🧪 **测试验证**

### **测试步骤**：

1. **启动程序**
2. **等待自动过滤** 或 **点击"手动过滤"**
3. **查看控制台输出**：

```
[过滤开始] 总股票数: 5000, 目标日期: 2026-01-20
【强多排列】（过滤器1）开始执行...
【中多排列】（过滤器2）开始执行...
【强多缠绕】（过滤器3）开始执行...
【中多缠绕】（过滤器4）开始执行...
【强多反弹】（过滤器5）开始执行...
【中多反弹】（过滤器6）开始执行...

【强多排列】（过滤器1）完成 - 结果: 15 只股票，耗时: 18.32 秒
【中多排列】（过滤器2）完成 - 结果: 45 只股票，耗时: 19.12 秒
【强多缠绕】（过滤器3）完成 - 结果: 22 只股票，耗时: 18.98 秒
【中多缠绕】（过滤器4）完成 - 结果: 68 只股票，耗时: 20.45 秒
【强多反弹】（过滤器5）完成 - 结果: 12 只股票，耗时: 17.88 秒
【中多反弹】（过滤器6）完成 - 结果: 35 只股票，耗时: 18.67 秒

所有过滤器执行完成！
```

---

## 🎯 **关键变化总结**

| 项目 | 修改前 | 修改后 |
|------|--------|--------|
| **过滤器数量** | 8个 | 6个 |
| **参数数量** | 3个（M1/M2/M3） | 4个（M1/M2/M3/M4） |
| **条件1** | 强多金叉 | **强多排列** |
| **条件2** | 中多金叉 | **中多排列** |
| **条件3** | 强多排列 | **强多缠绕** |
| **条件4** | 中多排列 | **中多缠绕** |
| **条件5** | 强多缠绕 | **强多反弹** |
| **条件6** | 中多缠绕 | **中多反弹** |
| **条件7** | 强多反弹 | **已删除** |
| **条件8** | 中多反弹 | **已删除** |

---

## ✅ **完成状态**

- ✅ 添加M4参数（默认30）
- ✅ 更新CheckCondition逻辑（6个新条件）
- ✅ 修改FilterService（从8个改为6个）
- ✅ 更新UnifiedStockFilter注释
- ✅ 编译成功
- ✅ 程序已重启

---

现在程序已经使用新的6个过滤条件，可以开始测试了！🎉
