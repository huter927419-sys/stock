# HaiLiDrv 与 MairuiStockMonitor（龙卷风版）对齐清单

**目标**：HaiLiDrv（数据源 = MQ）与 F:\dsfr\C2\HaiLiDrvDemo（MairuiStockMonitor，数据源 = 龙卷风/麦蕊 API）的**菜单、界面选项、股票/板块配置完全一致**，仅数据源不同。

---

## 一、原版 MairuiStockMonitor 已具备项

### 1. 菜单栏（实际显示的 3 项）

| 菜单 | 子项 | 说明 |
|-----|------|------|
| **连接** | 启动接收 | 开始接收行情 |
| | 停止接收 | 停止接收 |
| | --- | 分隔线 |
| | 退出应用 | 关闭程序 |
| **主题** | 深色主题 | 勾选，默认 |
| | 浅色主题 | 勾选互斥 |
| **筛选** | 全局筛选设置 | 打开筛选对话框 |

注：原版 Designer 里还有「数据」「视图」菜单（加载基础数据、创建板块、显示数据面板、显示日志面板），但 **menuStrip1 只绑定了 连接/主题/筛选**，若你希望也完全一致，可再补上「数据」「视图」。

### 2. 界面布局

- **主区域**：上方 = 数据面板（DataGridView + 标题），下方 = 右侧再分割为 **F10 面板** + **日志面板**。
- **工具栏按钮**：启动接收、加载基础数据、加载分钟数据、加载 F10、清空日志。
- **视图选项**：菜单「显示数据面板」「显示日志面板」勾选控制显示/隐藏。

### 3. 筛选配置（FilterSettings）

- **涨幅筛选**：启用勾选 + 最小涨幅（%），默认 3。
- **日内涨幅筛选**：启用勾选 + 最小日内涨幅（%）（现价相对开盘价），默认 5。
- **逻辑**：多条件为 **OR**，满足其一即通过。
- **对话框**：标题「数据筛选设置」，深色背景，确定/取消/重置。

### 4. 板块配置（BoardManager + Boards.json）

- **路径**：`Config/Boards.json`（相对 exe 或可配置）。
- **单板块结构**：`Name`, `StockCodes[]`, `StockNames{ code: name }`, `Width`, `Height`。
- **多板块**：多个上述对象组成数组；每板块对应一个「板块面板」（StockBoardPanel），显示该板块股票列表。

### 5. 主题

- 深色：背景 #1E1E1E / #2D2D30，前景白/灰。
- 浅色：对应浅色配色。
- 切换后整体应用（窗体、面板、表格、按钮等）。

---

## 二、HaiLiDrv 当前状态（mqq 内）

- **无菜单栏**，仅有工具栏：刷新数据、配置股票代码、搜索、状态。
- **无**「连接 / 主题 / 筛选」菜单及对应逻辑。
- **无** FilterSettings、无「全局筛选设置」对话框。
- **无** BoardManager/Boards.json，板块用 `HaiLiDrv_PanelCount` + `HaiLiDrv_Panel{i}_StockCodes` 配置。
- **无** 数据表格主区、无日志面板、无 F10 面板；仅有 WrapPanel 多块 HaiLiDrvPanel。
- **数据源**：已从 MQ（RealTimeDataCache）获取，无需改。

---

## 三、要对齐的项（实现时检查）

1. **菜单**
   - [ ] 增加菜单栏：**连接**（启动接收、停止接收、退出应用）、**主题**（深色主题、浅色主题）、**筛选**（全局筛选设置）。
   - [ ] 若需与原版 100% 一致：再增加 **数据**（加载基础数据、创建板块）、**视图**（显示数据面板、显示日志面板）。

2. **界面**
   - [ ] 布局：主区 = 数据表格 + 下方分割 = 日志面板（+ 可选 F10）；与原版布局一致或等价。
   - [ ] 视图选项：通过菜单勾选控制「显示数据面板」「显示日志面板」的显示/隐藏。
   - [ ] 主题：深色/浅色切换，全局应用。

3. **筛选**
   - [ ] 引入与 MairuiStockMonitor **相同**的 FilterSettings（涨幅筛选 + 日内涨幅筛选，OR 关系）。
   - [ ] 增加「全局筛选设置」对话框（文案、控件、逻辑与原版 FilterSettingsDialog 一致）。
   - [ ] 列表/表格展示数据时按 FilterSettings 过滤。

4. **板块与股票配置**
   - [ ] 支持 **Boards.json**：结构、路径与 MairuiStockMonitor 的 BoardManager 一致（Name, StockCodes, StockNames, Width, Height）。
   - [ ] 板块列表从 Boards.json 加载；每板块一个面板，面板内股票列表与名称来自该板块配置。
   - [ ] 可与现有 HaiLiDrv_Panel* 配置兼容（例如：无 Boards.json 时退化为当前 PanelCount + Panel 配置）。

5. **数据源（唯一差异）**
   - [ ] 保持 HaiLiDrv 从 **MQ（RealTimeDataCache）** 获取数据；不接入龙卷风/麦蕊 API。

---

## 四、建议实现顺序

1. 配置与模型：在 mqq 内新增/复用 FilterSettings、BoardConfig、BoardManager（或适配 Boards.json）。
2. 菜单 + 筛选：HaiLiDrv 菜单栏 + 「全局筛选设置」对话框 + 应用 FilterSettings 到数据展示。
3. 板块：Boards.json 的读/写与 HaiLiDrv 面板绑定（加载板块列表、按板块显示股票）。
4. 界面布局与视图：数据表格 + 日志面板（+ 可选 F10）+ 显示/隐藏菜单项。
5. 主题：深色/浅色切换并应用到 HaiLiDrv 整窗。

如你确认「菜单、界面选项、股票配置」就按上面 1:1 对齐（且仅数据源不同），我就按此清单在 mqq 的 HaiLiDrv 里逐项实现；若有要删减或补充的项，直接说即可。
