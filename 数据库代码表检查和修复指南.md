# 数据库代码表检查和修复指南

## 📋 问题背景

用户反馈：
1. **股票名称显示错误**：部分股票显示代码而不是名称
2. **非A股股票出现**：指数、基金、B股等出现在过滤结果中

**根本原因**：`stock_info` 表的数据可能存在问题

---

## 🔍 检查方法

我已经创建了两种检查工具，您可以选择其中一种使用：

### 方法1：使用SQL脚本（推荐）

**步骤**：
1. 双击运行 `db/diagnose_stock_info.bat`
2. 查看诊断结果

**或者在pgAdmin中执行**：
1. 打开 `db/diagnose_stock_info.sql`
2. 复制内容到pgAdmin查询窗口
3. 执行查看结果

### 方法2：使用C#诊断工具

**步骤**：
1. 双击运行 `tools/diagnose_stock_info.bat`
2. 自动编译并运行诊断工具
3. 查看详细报告

---

## 📊 诊断内容

### 1. 基本统计
- 总记录数
- 激活/停用状态
- 有名称/无名称统计

### 2. 市场分类统计
- 上海市场（600/601/603/605/688）
- 深圳市场（000/001/002/003/004/300/301）
- 北京市场（43/83/87/88）
- 每个市场的名称覆盖率

### 3. 可疑代码检查
- ❌ 指数代码（如000001上证指数）
- ❌ 基金代码（如000139）
- ❌ B股代码（200、900开头）

### 4. 市场代码错误
- 检查 `market_code` 字段是否与股票代码匹配

### 5. 名称异常
- 名称缺失（NULL、空字符串、等于代码）
- 名称过长（>8字符，可能是公司全称）
- ST股票检查

### 6. 重复代码检查
- 同一个股票代码是否有多条记录

### 7. 抽样检查
- 随机检查10只股票的数据

### 8. 总结和建议
- 数据完整性评估
- 名称完整性评估
- 市场代码准确性评估
- 修复建议

---

## ✅ 预期结果

### 正常的 stock_info 表应该满足：

| 指标 | 标准 | 说明 |
|------|------|------|
| 总记录数 | > 3000 | A股市场约4000-5000只股票 |
| 激活率 | > 80% | 大部分股票应该是激活状态 |
| 名称覆盖率 | > 90% | 绝大部分股票应该有名称 |
| 市场代码准确 | 0 错误 | market_code 应该与代码匹配 |
| 无指数/B股 | 0 条 | 不应该有指数、基金、B股 |
| 无重复代码 | 0 条 | 每个代码应该只有一条记录 |

---

## 🔧 常见问题及修复

### 问题1：发现指数/基金/B股代码

**症状**：
```
⚠ 000001 - 上证指数 (应该过滤)
⚠ 000139 - 富国国企债基金 (应该过滤)
⚠ 200开头的B股
```

**修复方法**：
执行 `db/fix_stock_info_common_issues.sql` 脚本，它会自动：
- 将指数代码标记为 `is_active = FALSE`
- 将B股代码标记为 `is_active = FALSE`
- 将已知的已退市股票标记为 `is_active = FALSE`

---

### 问题2：市场代码错误

**症状**：
```
代码       当前市场   应该是
600036    深圳(0)    上海(1)
000001    上海(1)    深圳(0)
```

**修复方法**：
执行 `db/fix_stock_info_common_issues.sql`，它会自动修复：
```sql
UPDATE stock_info
SET market_code = CASE
    WHEN stock_code ~ '^(600|601|603|605|688|900)' THEN 1  -- 上海
    WHEN stock_code ~ '^(000|001|002|003|004|300|301|200)' THEN 0  -- 深圳
    WHEN stock_code ~ '^(43|83|87|88)' THEN 2  -- 北京
    ELSE 0
END;
```

---

### 问题3：大量股票名称缺失

**症状**：
```
⚠ 000123 - (NULL)
⚠ 600456 - 600456 (名称等于代码)
... (共200-500只股票)
```

**原因**：
1. `stock_info` 表数据不完整
2. 没有从日线数据同步

**修复方法**：

#### 方法A：从日线数据同步（推荐）

在pgAdmin中执行：
```sql
-- 从日线数据同步股票代码到 stock_info
INSERT INTO stock_info (stock_code, stock_name, market_code, market_name, stock_type, is_active)
SELECT DISTINCT ON (stock_code)
    stock_code,
    stock_code AS stock_name,  -- 先用代码占位
    CASE
        WHEN stock_code ~ '^(600|601|603|605|688|900)' THEN 1
        WHEN stock_code ~ '^(000|001|002|003|004|300|301|200)' THEN 0
        WHEN stock_code ~ '^(43|83|87|88)' THEN 2
        ELSE 0
    END AS market_code,
    CASE
        WHEN stock_code ~ '^(600|601|603|605|688|900)' THEN '上海'
        WHEN stock_code ~ '^(000|001|002|003|004|300|301|200)' THEN '深圳'
        WHEN stock_code ~ '^(43|83|87|88)' THEN '北京'
        ELSE '深圳'
    END AS market_name,
    '股票' AS stock_type,
    TRUE AS is_active
FROM stock_daily_data
WHERE stock_code IS NOT NULL
  AND stock_code ~ '^[0-9]{6}$'
ON CONFLICT (stock_code) DO NOTHING;
```

#### 方法B：程序自动同步

在应用程序中调用：
```csharp
StockInfoCache.Instance.SyncFromDailyData();
```

或者重启应用程序，它会自动检查并同步。

#### 方法C：手动补充常见股票名称

编辑 `src/Core/Cache/StockInfoCache.cs` 的内置字典，添加缺失的股票名称。

---

### 问题4：重复代码

**症状**：
```
代码       出现次数
000001    2
600036    3
```

**原因**：
可能由于多次导入数据导致

**修复方法**：
```sql
-- 删除重复记录，只保留最新的一条
DELETE FROM stock_info
WHERE id NOT IN (
    SELECT MAX(id)
    FROM stock_info
    GROUP BY stock_code
);
```

---

## 🚀 快速修复流程

### 步骤1：诊断

```bash
# 运行诊断工具
db/diagnose_stock_info.bat

# 或者
tools/diagnose_stock_info.bat
```

### 步骤2：修复常见问题

```bash
# 在pgAdmin中执行
db/fix_stock_info_common_issues.sql
```

这个脚本会自动：
1. ✅ 修复市场代码错误
2. ✅ 停用指数代码
3. ✅ 停用B股代码
4. ✅ 停用已退市股票
5. ✅ 修复空名称（临时用代码代替）

### 步骤3：补充缺失名称（可选）

如果仍有大量股票名称缺失，可以：

#### 选项A：使用内置字典
编辑 `src/Core/Cache/StockInfoCache.cs` 的 `GetBuiltinStockName` 方法，添加缺失的名称。

#### 选项B：从实时数据同步
运行应用程序，等待从实时数据获取名称后自动更新到 `stock_info` 表。

#### 选项C：手动更新数据库
```sql
UPDATE stock_info SET stock_name = '平安银行' WHERE stock_code = '000001';
UPDATE stock_info SET stock_name = '招商银行' WHERE stock_code = '600036';
-- ... 更多
```

### 步骤4：验证

再次运行诊断工具，检查是否修复成功。

### 步骤5：重启应用

修复完成后，重启应用程序以重新加载 `StockInfoCache`。

---

## 📝 维护建议

### 1. 定期检查

建议每月运行一次诊断工具，确保数据质量。

### 2. 新股上市

当有新股上市时：
1. 运行应用程序，会自动从日线数据同步
2. 或手动添加到 `stock_info` 表

### 3. 股票退市

当股票退市时：
```sql
UPDATE stock_info
SET is_active = FALSE
WHERE stock_code = '退市代码';
```

### 4. 名称变更（ST加冠）

当股票被ST或摘帽时：
```sql
UPDATE stock_info
SET stock_name = '*ST步森'
WHERE stock_code = '002569';
```

或者更新内置字典。

---

## 🎯 最佳实践

### 1. 数据源优先级

程序获取股票名称的优先级：
```
1. 内置字典（GetBuiltinStockName）
   ↓ （如果没找到）
2. 内存缓存（_stockNameCache）
   ↓ （如果没找到）
3. 数据库查询（stock_info 表）
   ↓ （如果没找到）
4. 返回股票代码
```

### 2. 确保数据一致性

- `stock_info.market_code` 应该与股票代码匹配
- `stock_info.is_active` 应该标记有效股票
- 黑名单（`StockDataParser.InvalidStockCodes`）应该包含所有非A股代码

### 3. 自动化流程

应用程序已经实现了自动化：
1. ✅ 启动时自动加载 `StockInfoCache`
2. ✅ 如果缓存为空，自动从日线数据同步
3. ✅ 加载时自动过滤非A股股票
4. ✅ 优先使用内置字典中的正确名称

---

## 📂 相关文件

### 诊断工具
- `db/diagnose_stock_info.sql` - SQL诊断脚本
- `db/diagnose_stock_info.bat` - SQL脚本执行器
- `tools/StockInfoDiagnostics.cs` - C#诊断工具
- `tools/diagnose_stock_info.bat` - C#工具编译运行器

### 修复脚本
- `db/fix_stock_info_common_issues.sql` - 常见问题修复脚本

### 相关代码
- `src/Core/Cache/StockInfoCache.cs` - 股票信息缓存
- `src/Core/Helpers/StockDataParser.cs` - 股票代码验证（黑名单）
- `src/Filters/FilterService.cs` - 过滤服务（加载逻辑）

---

## 🎉 总结

### 完整检查和修复流程

1. **诊断** → 运行 `db/diagnose_stock_info.bat`
2. **修复** → 执行 `db/fix_stock_info_common_issues.sql`
3. **验证** → 再次运行诊断工具
4. **重启** → 重启应用程序
5. **测试** → 刷新过滤，检查结果

### 预期效果

修复后，`stock_info` 表应该：
- ✅ 只包含A股（无指数/基金/B股）
- ✅ 市场代码正确
- ✅ 绝大部分股票有名称
- ✅ 无重复代码

---

**修复完成后，请重启应用程序并刷新过滤，应该能看到显著改善！** 📊✨
