# 为什么一个股票都没有过滤出来 - 诊断指南

## 问题现象

批量KD计算完成后，6个过滤条件都没有过滤出任何股票。

## 诊断步骤

### 第1步：查看控制台输出的诊断信息

运行程序后，查找以下关键日志：

#### 1.1 批量查询统计
```
[性能优化] 批量查询成交金额和换手率: XXXX只股票, 查询到: YYYY条数据
```

**检查项：**
- 如果 `查询到: 0条数据` → **成交金额数据缺失**
- 如果查询到的数据很少 → **成交金额数据不完整**

**原因：**
- 数据库中 `daily_data` 表的 `amount` 字段为空或null
- 昨日日期没有数据

**解决方案：**
```sql
-- 检查成交金额数据
SELECT trade_date, COUNT(*) as count,
       SUM(CASE WHEN amount IS NOT NULL THEN 1 ELSE 0 END) as has_amount
FROM daily_data
WHERE trade_date >= CURRENT_DATE - INTERVAL '5 days'
GROUP BY trade_date
ORDER BY trade_date DESC;
```

---

#### 1.2 条件5/6诊断信息（自动输出）
```
[表格5/6诊断] 在「有今日+昨日KD 且 昨天成交金额>=N亿」的股票中，各子条件满足数：
  候选数(有KD且金额达标): 0    ← 关键！
  条件5(强多反弹): A2>=M1:0  K1<M3:0  K1<A2:0  全部:0
  条件6(中多反弹): 65<=A2<78:0  K1<M4:0  K1<A2:0  全部:0
```

**检查项：**

##### 情况A：候选数 = 0
说明**没有任何股票**同时满足：
1. 今日 KD 有值
2. 昨日 KD 有值
3. 昨日成交金额 >= N亿（默认5亿）

**可能原因：**
- ✅ KD数据未计算出来（数据不足）
- ✅ 成交金额数据缺失
- ✅ 目标日期不一致（批量预计算的日期 ≠ 过滤使用的日期）

**排查方法：**
```
1. 查看批量KD计算的统计：
   [批量KD计算] 预计算完成！
     成功: 3800 只
     数据不足: 446 只    ← 如果成功数很少，KD数据问题

2. 查看成交金额查询：
   [性能优化] 批量查询成交金额和换手率: 4246只股票, 查询到: 0条数据
   ← 如果查询到0条，成交金额数据缺失

3. 检查目标日期：
   [目标日期] 2026-01-30（批量预计算与 6 个条件均使用此日期）
   ← 确保日期一致
```

##### 情况B：候选数 > 0，但全部 = 0
说明有候选股票，但**条件判断不满足**。

**示例：**
```
  候选数(有KD且金额达标): 3800
  条件5(强多反弹): A2>=M1:0  K1<M3:1200  K1<A2:800  全部:0
```

**分析：**
- A2>=M1:0 → 没有月K和季K同时 >= 78的股票
- 所有股票都在这一条件上被卡住了

**可能原因：**
1. **阈值设置过高**：M1=78 可能太高，当前市场没有这么强的股票
2. **数据周期不够**：月K和季K需要足够长的历史数据

---

##### 情况C：候选数 > 0，全部 > 0，但结果 = 0
说明有股票满足所有条件，但**被涨幅过滤掉了**。

**示例：**
```
  候选数(有KD且金额达标): 3800
  条件5(强多反弹): A2>=M1:50  K1<M3:1200  K1<A2:800  全部:20

[计算完成] 处理: 4246只, 符合条件: 0只
[涨幅过滤] 过滤掉涨幅>7%的股票: 20只    ← 关键！
```

**原因：**
- 所有满足条件的股票涨幅都超过了阈值（默认7%）

**解决方案：**
```
调整涨幅过滤阈值：
PriceChangeFilterThreshold = 10   (从7%增加到10%)
```

---

### 第2步：检查配置参数

打开配置文件，检查阈值设置：

#### 2.1 全局阈值（NewFilterConditions.cs 默认值）
```
GlobalThreshold_M1 = 78    # 强多阈值
GlobalThreshold_M2 = 65    # 中多阈值
GlobalThreshold_M3 = 50    # 反弹上限
GlobalThreshold_M4 = 30    # 中多反弹上限
GlobalThreshold_N = 5      # 成交金额门槛（5亿）
```

**诊断：**
- M1=78 太高 → 降低到 70 或 65
- M2=65 太高 → 降低到 60 或 55
- N=5 太高 → 降低到 3 或 2（3亿或2亿）

#### 2.2 涨幅过滤阈值
```
PriceChangeFilterThreshold = 7   # 涨幅上限（7%）
```

**诊断：**
- 如果条件满足但被涨幅过滤掉 → 增加到 10 或更高

---

### 第3步：检查数据完整性

#### 3.1 检查KD数据
```sql
-- 查看某只股票的KD数据（以000001为例）
SELECT stock_code, trade_date, weekly_k, monthly_k, quarterly_k
FROM kd_values
WHERE stock_code = '000001'
ORDER BY trade_date DESC
LIMIT 10;
```

**预期：**
- 应该有近期的 weekly_k, monthly_k, quarterly_k 值
- 如果为 null 或空 → KD计算失败

#### 3.2 检查成交金额数据
```sql
-- 查看最近几天的成交金额数据
SELECT trade_date,
       COUNT(*) as total_stocks,
       SUM(CASE WHEN amount IS NOT NULL AND amount > 500000000 THEN 1 ELSE 0 END) as amount_gt_5yi,
       AVG(amount) / 100000000 as avg_amount_yi
FROM daily_data
WHERE trade_date >= CURRENT_DATE - INTERVAL '5 days'
GROUP BY trade_date
ORDER BY trade_date DESC;
```

**预期：**
- amount_gt_5yi > 0（应该有成交金额超过5亿的股票）
- 如果 = 0 → 成交金额数据问题或阈值太高

#### 3.3 检查日线数据
```sql
-- 查看最近的日线数据
SELECT trade_date, COUNT(*) as count
FROM daily_data
WHERE trade_date >= CURRENT_DATE - INTERVAL '10 days'
GROUP BY trade_date
ORDER BY trade_date DESC;
```

**预期：**
- 应该有最近几天的数据（每天3000-4000条左右）
- 如果缺失 → 日线数据未入库

---

### 第4步：手动测试单个条件

创建一个测试脚本，手动检查某只股票是否满足条件：

```csharp
// 测试条件5：强多反弹
// K1<A2 AND A2>=M1 AND K1<M3

string stockCode = "000001"; // 替换为你要测试的股票
DateTime targetDate = DateTime.Parse("2026-01-30");

// 获取KD值
var weeklyKD = _chartService.GetKDValue(stockCode, targetDate, "week");
var monthlyKD = _chartService.GetKDValue(stockCode, targetDate, "month");
var quarterlyKD = _chartService.GetKDValue(stockCode, targetDate, "quarter");

if (weeklyKD != null && monthlyKD != null && quarterlyKD != null)
{
    decimal k1 = weeklyKD.K;
    decimal k2 = monthlyKD.K;
    decimal k3 = quarterlyKD.K;
    decimal a2 = Math.Min(k2, k3);

    Console.WriteLine($"股票: {stockCode}");
    Console.WriteLine($"K1(周K): {k1:F2}");
    Console.WriteLine($"K2(月K): {k2:F2}");
    Console.WriteLine($"K3(季K): {k3:F2}");
    Console.WriteLine($"A2(MIN(K2,K3)): {a2:F2}");
    Console.WriteLine($"");
    Console.WriteLine($"条件5检查：");
    Console.WriteLine($"  K1 < A2: {k1 < a2} ({k1:F2} < {a2:F2})");
    Console.WriteLine($"  A2 >= M1: {a2 >= 78} ({a2:F2} >= 78)");
    Console.WriteLine($"  K1 < M3: {k1 < 50} ({k1:F2} < 50)");
    Console.WriteLine($"  结果: {k1 < a2 && a2 >= 78 && k1 < 50}");
}
else
{
    Console.WriteLine($"股票 {stockCode} 的KD数据缺失");
}
```

---

### 第5步：常见问题及解决方案

| 问题 | 原因 | 解决方案 |
|-----|------|---------|
| **候选数 = 0** | 成交金额数据缺失 | 检查数据库 amount 字段，确保MQ服务接收了日线数据 |
| **候选数 = 0** | KD数据未计算 | 检查批量KD计算是否成功，数据不足股票数量 |
| **候选数 = 0** | 目标日期不一致 | 确保批量预计算和过滤使用同一日期 |
| **A2>=M1 = 0** | M1阈值太高(78) | 降低到 70、65 或更低 |
| **全部>0但结果=0** | 涨幅过滤太严格 | 增加 PriceChangeFilterThreshold 到 10 或 15 |
| **候选数很少** | N阈值太高(5亿) | 降低到 3亿 或 2亿 |

---

### 第6步：临时放宽条件测试

为了快速验证系统是否工作，可以临时放宽条件：

```ini
# 配置文件修改（临时测试）
GlobalThreshold_M1 = 50   # 从78降到50
GlobalThreshold_M2 = 40   # 从65降到40
GlobalThreshold_M3 = 30   # 从50降到30
GlobalThreshold_M4 = 20   # 从30降到20
GlobalThreshold_N = 1     # 从5降到1（1亿）
PriceChangeFilterThreshold = 20  # 从7%增加到20%
```

**预期结果：**
- 如果仍然 = 0 → 数据问题（KD或成交金额）
- 如果有结果 → 原阈值设置过严格

---

## 快速诊断清单

运行程序后，依次检查：

- [ ] `[批量查询成交金额和换手率]` 查询到的数据 > 0
- [ ] `[批量KD计算]` 成功数量 > 2000
- [ ] `[表格5/6诊断] 候选数` > 0
- [ ] `[表格5/6诊断]` 各子条件满足数 > 0
- [ ] `[涨幅过滤]` 过滤掉的数量 < 全部满足数量
- [ ] 配置文件中的阈值合理

如果以上都通过，但仍然没有结果，请提供完整的诊断日志输出。

---

## 开启详细诊断

在配置文件中添加：

```ini
FilterDiagnose_56 = true   # 开启条件5/6详细诊断
```

这将输出前10个失败候选的详细值，帮助排查问题：

```
[条件5详细] 000001: K1=45.23 A2=72.50 检查: K1<A2=true A2>=M1=false K1<M3=true
[条件5详细] 000002: K1=55.67 A2=80.12 检查: K1<A2=true A2>=M1=true K1<M3=false
```

从输出中可以看出具体哪个条件卡住了。
