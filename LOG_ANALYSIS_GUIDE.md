# 过滤执行日志分析指南

## 日志位置

### 1. 控制台输出
- **位置**：运行程序的控制台窗口
- **内容**：所有 `Console.WriteLine()` 的输出
- **特点**：实时显示，关闭程序后丢失

### 2. 日志面板（UI）
- **位置**：主窗口的日志面板
- **内容**：部分关键日志
- **特点**：可以查看历史日志

## 关键日志节点

### 1. 过滤开始
```
========== 开始执行KD过滤 ==========
时间: 2025-01-27 14:30:00
缓存中的股票数量: 5000
```

**检查点**：
- ✅ 股票数量是否正常（应该 > 0）
- ✅ 时间是否正确

### 2. 数据状态
```
当前时段: 盘中
数据策略: 数据库+实时数据
目标日期: 2025-01-27
数据库最新日期: 2025-01-27
实时缓存数量: 5000
实时数据状态: 新鲜
```

**检查点**：
- ✅ 目标日期是否正确
- ✅ 数据策略是否合理
- ✅ 实时数据是否新鲜

### 3. 过滤器执行
```
开始并行执行8个过滤条件...
股票数量: 5000

[14:30:00] 【强多金叉】（过滤器1）开始执行...
[14:30:00] 【中多金叉】（过滤器2）开始执行...
...
```

**检查点**：
- ✅ 8个过滤器是否都开始执行
- ✅ 开始时间是否相近（并行执行）

### 4. 进度信息
```
  进度: 500/5000 (10.0%), 已找到: 15 只
  进度: 1000/5000 (20.0%), 已找到: 32 只
  ...
```

**检查点**：
- ✅ 进度是否在增长
- ✅ 是否卡在某个进度点
- ✅ 找到的股票数量是否合理

### 5. 过滤器完成
```
[14:30:15] 【强多金叉】（过滤器1）完成
  结果数量: 25 只股票
  处理时间: 15.23 秒
```

**检查点**：
- ✅ 所有8个过滤器是否都完成
- ✅ 处理时间是否合理（通常 < 60秒）
- ✅ 结果数量是否正常

### 6. 错误信息
```
错误: 执行过滤器1（强多金叉）时出错: [异常信息]
异常堆栈: [堆栈信息]
```

**检查点**：
- ⚠️ 是否有异常
- ⚠️ 异常类型和原因
- ⚠️ 哪个过滤器出错

## 常见问题分析

### 问题1：没有数据输出

**症状**：
- 控制台显示"开始执行"，但没有后续输出
- 进度信息不更新

**可能原因**：
1. **KD计算卡住**：某只股票的KD计算耗时过长
2. **数据库连接问题**：查询数据库时卡住
3. **内存不足**：并行执行导致内存耗尽
4. **死锁**：多线程竞争资源导致死锁

**排查方法**：
```bash
# 查看是否有异常输出
# 检查控制台是否有错误信息

# 检查数据库连接
# 查看是否有数据库连接超时错误
```

### 问题2：只有部分过滤器完成

**症状**：
- 只有面板8有结果
- 其他面板没有输出

**可能原因**：
1. **异常中断**：前面的过滤器抛出异常
2. **条件过严**：过滤条件太严格，没有股票满足
3. **数据不足**：某些股票的KD数据不足

**排查方法**：
```bash
# 查看是否有异常信息
# 检查每个过滤器的完成时间
# 检查结果数量是否为0
```

### 问题3：执行时间过长

**症状**：
- 过滤执行超过5分钟
- 进度更新很慢

**可能原因**：
1. **首次加载**：首次执行需要从数据库加载K线数据
2. **缓存未命中**：K线数据缓存未命中，需要查询数据库
3. **CPU负载高**：并行度设置过高，导致上下文切换开销

**排查方法**：
```bash
# 查看K线缓存统计
[K线缓存统计] 缓存股票数: 0, 总数据点数: 0
# 如果缓存数为0，说明是首次加载

# 查看处理时间
处理时间: 300.00 秒  # 如果超过60秒，可能有问题
```

### 问题4：内存不足

**症状**：
- 程序崩溃
- 系统变慢
- 内存使用率很高

**可能原因**：
1. **并行度过高**：同时处理太多股票
2. **缓存过大**：K线数据缓存占用过多内存
3. **内存泄漏**：未释放的资源

**排查方法**：
```bash
# 查看内存使用
# 使用任务管理器查看内存占用

# 查看缓存统计
[K线缓存统计] 缓存股票数: 10000, 总数据点数: 10,000,000
# 如果缓存数过大，可能需要清理
```

## 日志分析脚本

### PowerShell 脚本（分析控制台输出）

```powershell
# 保存控制台输出到文件
# 运行程序时：program.exe > filter_log.txt 2>&1

# 分析日志
$log = Get-Content filter_log.txt

# 查找错误
$errors = $log | Select-String "错误|异常|Exception"
Write-Host "错误数量: $($errors.Count)"
$errors

# 查找过滤器执行时间
$filterTimes = $log | Select-String "处理时间"
$filterTimes

# 查找结果数量
$results = $log | Select-String "结果数量"
$results

# 查找进度信息
$progress = $log | Select-String "进度:"
$progress | Select-Object -Last 10
```

### 关键信息提取

```powershell
# 提取所有过滤器的执行时间
$log | Select-String "处理时间" | ForEach-Object {
    if ($_ -match "过滤器(\d+).*处理时间: ([\d.]+) 秒") {
        Write-Host "过滤器$($matches[1]): $($matches[2]) 秒"
    }
}

# 提取所有过滤器的结果数量
$log | Select-String "结果数量" | ForEach-Object {
    if ($_ -match "过滤器(\d+).*结果数量: (\d+)") {
        Write-Host "过滤器$($matches[1]): $($matches[2]) 只股票"
    }
}
```

## 性能基准

### 正常执行时间（参考）

| 股票数量 | 预期时间 | 说明 |
|---------|---------|------|
| 1000 | 10-20秒 | 首次执行（需要加载数据） |
| 1000 | 5-10秒 | 后续执行（使用缓存） |
| 5000 | 30-60秒 | 首次执行 |
| 5000 | 15-30秒 | 后续执行 |

### 异常情况

| 症状 | 可能原因 | 解决方案 |
|------|---------|---------|
| 超过5分钟 | 数据库查询慢 | 检查数据库连接和索引 |
| 卡在某个进度 | 某只股票计算卡住 | 检查是否有异常股票 |
| 内存占用高 | 缓存过大 | 清理缓存或减少并行度 |

## 快速诊断清单

### ✅ 检查清单

- [ ] 控制台是否有输出？
- [ ] 8个过滤器是否都开始执行？
- [ ] 进度信息是否在更新？
- [ ] 是否有错误信息？
- [ ] 所有过滤器是否都完成？
- [ ] 结果数量是否正常？
- [ ] 执行时间是否合理？

### 🔍 如果卡住

1. **检查控制台最后一行**：看卡在哪里
2. **检查进度信息**：看卡在哪个进度点
3. **检查错误信息**：看是否有异常
4. **检查数据库连接**：看是否数据库查询卡住
5. **检查内存使用**：看是否内存不足

## 提供日志的方法

### 方法1：复制控制台输出
1. 运行程序
2. 复制控制台的所有输出
3. 粘贴到文本文件
4. 发送给我分析

### 方法2：重定向到文件
```bash
# Windows CMD
program.exe > filter_log.txt 2>&1

# PowerShell
program.exe *> filter_log.txt
```

### 方法3：使用日志面板
1. 打开主窗口的日志面板
2. 复制日志内容
3. 保存到文件

## 需要提供的信息

分析日志时，请提供：
1. **完整的控制台输出**（从开始到结束）
2. **执行时间**（从开始到卡住/完成的时间）
3. **股票数量**（缓存中的股票数量）
4. **错误信息**（如果有）
5. **系统信息**（CPU核心数、内存大小）

---

**请将日志内容提供给我，我会帮您分析问题原因！**
