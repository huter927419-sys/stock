# 手动更新股票代码指南

## 方式一：使用SQL文件

1. 打开命令行 (CMD)
2. 执行以下命令：

```cmd
cd F:\dsfr\mqq
set PGPASSWORD=123456
F:\dsfr\mqq\tools\bin\psql.exe -h localhost -p 8532 -U postgres -d stockdb -f db\quick_update_stocks.sql
```

---

## 方式二：复制粘贴SQL命令

1. 打开 pgAdmin 或任何 PostgreSQL 客户端
2. 连接到 `stockdb` 数据库
3. 复制下面的SQL命令并执行：

```sql
-- 标记无效代码为不活跃
UPDATE stock_info SET is_active = FALSE WHERE stock_code IN (
    '000091', '000102', '000132', '000137', '000146',
    '000071', '000033', '000052', '000053',
    '000073', '000077', '000107', '000161', '000847', '000854'
);

-- 确保000851是有效的A股
UPDATE stock_info SET stock_name = '高鸿股份', is_active = TRUE WHERE stock_code = '000851';

-- 更新常见股票名称
UPDATE stock_info SET stock_name = '平安银行' WHERE stock_code = '000001';
UPDATE stock_info SET stock_name = '万科A' WHERE stock_code = '000002';
UPDATE stock_info SET stock_name = '鲁西化工' WHERE stock_code = '000830';
UPDATE stock_info SET stock_name = '东莞控股' WHERE stock_code = '000828';
UPDATE stock_info SET stock_name = '贵州茅台' WHERE stock_code = '600519';
UPDATE stock_info SET stock_name = '同花顺' WHERE stock_code = '300033';

-- 查看更新结果
SELECT stock_code, stock_name, is_active FROM stock_info WHERE stock_code = '000851';
```

---

## 方式三：使用命令行直接执行

复制下面**整行命令**到CMD中执行：

```cmd
cd F:\dsfr\mqq && set PGPASSWORD=123456 && F:\dsfr\mqq\tools\bin\psql.exe -h localhost -p 8532 -U postgres -d stockdb -c "UPDATE stock_info SET is_active = FALSE WHERE stock_code IN ('000091','000102','000132','000137','000146','000071','000033','000052','000053','000073','000077','000107','000161','000847','000854'); UPDATE stock_info SET stock_name = '高鸿股份', is_active = TRUE WHERE stock_code = '000851'; SELECT stock_code, stock_name, is_active FROM stock_info WHERE stock_code = '000851';"
```

---

## 验证更新结果

执行以下SQL验证：

```sql
-- 1. 检查000851是否正确
SELECT stock_code, stock_name, is_active 
FROM stock_info 
WHERE stock_code = '000851';
-- 应该显示: 000851 | 高鸿股份 | true

-- 2. 检查无效代码是否已标记
SELECT stock_code, stock_name, is_active 
FROM stock_info 
WHERE stock_code IN ('000091', '000071', '000132') 
ORDER BY stock_code;
-- 这些应该显示 is_active = false

-- 3. 统计有效和无效代码数量
SELECT 
    is_active,
    COUNT(*) as 数量
FROM stock_info 
WHERE stock_code ~ '^[0-9]{6}$'
GROUP BY is_active;
```

---

## 如果需要更新更多股票名称

参考 `A股常见代码名称对照表.md` 文件，里面有200+常见股票的代码和名称对照表。

你可以按照以下格式批量更新：

```sql
UPDATE stock_info SET stock_name = '股票名称' WHERE stock_code = '代码';
```

例如：
```sql
UPDATE stock_info SET stock_name = '中兴通讯' WHERE stock_code = '000063';
UPDATE stock_info SET stock_name = '格力电器' WHERE stock_code = '000651';
UPDATE stock_info SET stock_name = '五粮液' WHERE stock_code = '000858';
-- ... 更多
```

---

## 常见问题

### Q: 如何查看哪些股票名称是错误的？

```sql
SELECT stock_code, stock_name 
FROM stock_info 
WHERE stock_name = stock_code  -- 名称等于代码，说明没有正确名称
  AND is_active = TRUE
ORDER BY stock_code
LIMIT 50;
```

### Q: 如何重置某个股票为无效？

```sql
UPDATE stock_info 
SET is_active = FALSE 
WHERE stock_code = '股票代码';
```

### Q: 如何批量更新？

将完整的 `db/update_stock_names_verified.sql` 文件导入数据库，它包含200+股票的名称更新。

```cmd
set PGPASSWORD=123456
F:\dsfr\mqq\tools\bin\psql.exe -h localhost -p 8532 -U postgres -d stockdb -f db\update_stock_names_verified.sql
```
