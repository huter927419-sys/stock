# 「没有计算出来」日志排查说明

## 1. 如何抓日志

### 方法一：用脚本（推荐）

```cmd
.\capture_kd_log.bat
```

按提示按键后，程序会运行并把**所有控制台输出**写入当前目录下的 `kd_log_日期_时间.txt`。  
计算一次后按 `Ctrl+C` 结束，日志即保存在该文件。

### 方法二：命令行重定向

```cmd
cd /d 项目根目录
bin\Release\MQReceiver.exe > kd_log.txt 2>&1
```

运行到计算完成（或你确认已复现「没有计算出来」）后 `Ctrl+C`，日志在 `kd_log.txt`。

---

## 2. 日志里重点看哪几行

打开上述日志文件，按顺序看：

| 位置 | 日志内容 | 说明 |
|------|----------|------|
| 开始计算后 | `[目标日期] yyyy-MM-dd（批量预计算与 6 个条件均使用此日期...）` | 确认目标日期一致，若和预期不符可能影响结果 |
| 性能优化块 | `[性能优化] 首次执行，正在初始化...` 或 `批量KD计算器已启用` 或 `使用标准模式` | 若一直是「标准模式」且无「批量KD计算器已启用」，说明预计算未启用，面板5/6 更容易无结果 |
| 每个条件完成 | `【强多反弹】（计算器5）完成 - 结果: N 只股票` | 若 N=0，说明条件5 本轮没有结果 |
| 所有条件完成后 | `[计算结果] 面板1:x 面板2:x ... 面板5:x 面板6:x` | **直接看各面板数量**，哪个为 0 一目了然 |
| 条件5/6 完成后 | `[表格5/6诊断] ...`、`候选数(有KD且金额达标): N` | 若**候选数=0**，表示没有「有 KD 且昨日成交额>=N 亿」的股票，多为 KD 未算出或成交额未达标 |
| 若候选数=0 | `[候选数=0] 没有股票同时满足...` | 会提示检查：1) 批量 KD 预计算与目标日期 2) 成交金额批量查询是否有数据 |

---

## 3. 根据日志判断原因

- **`[计算结果] 面板5:0 面板6:0`（或其一为 0）**
  - 往上看该条件（如【强多反弹】）后面的 **`[表格5/6诊断]`**：
    - **候选数=0**  
      → 没有股票同时满足「今日+昨日 KD 都有」且「昨日成交额>=N 亿」。  
      → 查：上方是否有「批量查询成交金额和换手率: 查到 x 条」、批量 KD 是否成功、`[目标日期]` 是否合理。
    - **候选数>0，但 A2>=M1 / K1<M3 / K1<A2 等某一项为 0**  
      → 条件公式不满足（例如没有月K/季K 同时>=78 的股票），属于数据/阈值问题，不是程序 bug。
    - **全部满足>0 但结果仍为 0**  
      → 可能被**涨幅过滤**掉（涨幅>阈值），可看是否有 `[涨幅计算] 计算掉涨幅>x%的股票: N只`。

- **没有出现 `[性能优化] 批量KD计算器已启用`，只有「使用标准模式」**
  - 说明批量预计算未启用（初始化失败或未执行），KD 依赖 ChartService 实时算，目标日期或缓存一旦不一致就容易出现面板5/6 无结果。  
  - 可看前面是否有 `[性能优化] ⚠️ 初始化内存缓存失败`，下面会有错误详情。

- **执行计算时出错**
  - 日志里会有 `执行计算时出错: ...` 和异常堆栈，按报错排查即可。

---

## 4. 已做的代码修复（便于对照日志）

- **BatchKDCalculator**：缓存 key 统一为 `targetDate.Date`，避免「同一天、时间不同」导致缓存未命中、KD 为 null。
- **FilterService**：目标日期只算一次，批量预计算与 6 个条件都用同一 `targetDateForInit`，并在日志中打出 `[目标日期]`。
- 计算结束后会打印 **`[计算结果] 面板1:... 面板6:...`**，并在面板5/6 为 0 时提示看「表格5/6诊断」和「候选数=0」说明。

按上面步骤抓一次日志，把 **`[目标日期]`、`[计算结果]`、以及对应条件5/6 的 `[表格5/6诊断]` 几段**贴出来，就可以进一步精确定位「为什么还没有计算出来」。
