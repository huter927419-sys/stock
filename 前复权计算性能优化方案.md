# 前复权计算性能优化方案

## 🎯 **优化目标**

在**保留前复权计算**的前提下，通过算法优化和并行策略调整，提升过滤性能。

---

## ✅ **已实施的优化**

### 1️⃣ **算法优化：减少重复计算**

#### **问题分析**

**优化前**：
```csharp
foreach (var kvp in ohlcData)  // 2000条K线
{
    // 每个日期都要筛选和排序除权数据
    var exRightsList = allExRights.Where(x => x.ExRightsDate > targetDate).ToList();
    foreach (var exRights in exRightsList.OrderByDescending(x => x.ExRightsDate))
    {
        // 计算复权价格
    }
}
```

**时间复杂度**：`O(n × m × log(m))`
- n = K线数量（约2000）
- m = 除权事件数量（约10个）
- 每个日期：筛选 O(m) + 排序 O(m log m) = **O(m log m)**
- 总计：**2000 × 10 × log(10) ≈ 66,000次操作**

#### **优化后**

```csharp
// 预先排序除权数据（只排序一次）
var sortedExRights = allExRights.OrderByDescending(x => x.ExRightsDate).ToList();

// 按日期排序K线数据
var sortedOhlc = ohlcData.OrderBy(x => x.Key).ToList();

foreach (var kvp in sortedOhlc)
{
    // 快速路径：如果没有除权影响，直接返回
    if (sortedExRights.Count > 0 && sortedExRights[sortedExRights.Count - 1].ExRightsDate <= targetDate)
    {
        result[targetDate] = ohlc;
        continue;
    }
    
    // 早停机制：一旦发现除权日期在目标日期之前就停止
    foreach (var exRights in sortedExRights)
    {
        if (exRights.ExRightsDate <= targetDate)
            break; // 早停，避免无效遍历
        
        // 计算复权价格
    }
}
```

**时间复杂度**：`O(m log m + n × m_avg)`
- 排序一次：O(m log m) ≈ 33次操作
- 每个日期：平均遍历 m/2 个除权事件
- 总计：**33 + 2000 × 5 ≈ 10,033次操作**

**性能提升**：**66,000 → 10,000** = **6.6倍提升**

---

### 2️⃣ **并行度优化：减少线程竞争**

#### **问题分析**

**优化前**：
```csharp
int optimalParallelism = Math.Min(Environment.ProcessorCount, 16);  // 通常是16线程
```

**问题**：
- 前复权计算是**内存密集型**操作（大量字典查询和缓存访问）
- 过度并行导致：
  - CPU缓存失效（Cache Thrashing）
  - 内存竞争（Memory Contention）
  - 线程切换开销（Context Switching）
  - GC压力增加

#### **优化后**

```csharp
// 对于IO/内存密集型操作，使用较保守的并行设置
int optimalParallelism = Math.Min(Environment.ProcessorCount / 2, 8);
if (optimalParallelism < 4) optimalParallelism = 4; // 至少4个线程
```

**策略**：
- **CPU核心数/2**：减少缓存竞争
- **最多8线程**：避免过度并行
- **最少4线程**：保证并行效率

| CPU核心数 | 优化前 | 优化后 |
|----------|--------|--------|
| 4核 | 4线程 | 4线程 |
| 8核 | 8线程 | **4线程** |
| 16核 | 16线程 | **8线程** |
| 32核 | 16线程 | **8线程** |

**效果**：减少线程切换和内存竞争，提升实际吞吐量。

---

### 3️⃣ **进度显示：改善用户体验**

```csharp
Console.WriteLine($"[过滤开始] 总股票数: {totalCount}, 目标日期: {targetDate:yyyy-MM-dd}");

// ... 处理过程中每500只输出进度
if (processedCount % 500 == 0)
{
    Console.WriteLine($"  进度: {processedCount}/{totalCount} ({progress:F1}%), 已找到: {validCount} 只");
}

// ... 完成后输出统计
Console.WriteLine($"[过滤完成] 处理: {totalCount}只, 符合条件: {validCount}只, 耗时: {elapsed:F1}秒, 速度: {totalCount/elapsed:F0}只/秒");
```

**好处**：
- 用户知道程序正在工作，而不是卡住
- 可以看到处理速度，评估剩余时间

---

## 📊 **性能提升预期**

### **理论分析**

| 优化项 | 提升倍数 |
|--------|---------|
| 算法优化（减少重复计算） | **6.6倍** |
| 并行度优化（减少竞争） | **1.5-2倍** |
| **综合效果** | **10-13倍** |

### **实际预期**

考虑到缓存、I/O等因素，实际提升可能在 **3-5倍**：

| 场景 | 优化前 | 优化后（预期） | 提升 |
|------|--------|---------------|------|
| **首次过滤（5000股）** | 60-120秒 | **20-40秒** | **3倍** ⚡️ |
| **二次过滤** | 30-60秒 | **10-20秒** | **3倍** ⚡️⚡️ |
| **后续刷新** | 15-30秒 | **5-10秒** | **3倍** ⚡️⚡️⚡️ |

---

## 🔍 **为什么不能更快？**

### **瓶颈分析**

1. **除权数据加载**：
   - 每只股票需要查询数据库加载除权数据
   - 即使有缓存，首次加载仍需时间

2. **复权计算本身**：
   - 每只股票约2000条K线 × 4个价格 = 8000次浮点运算
   - 5000只股票 = **4000万次**浮点运算

3. **内存分配**：
   - 大量字典、列表创建和销毁
   - GC（垃圾回收）压力

4. **数据库I/O**：
   - K线数据加载（即使有Repository缓存）
   - 首次加载需要查询数据库

---

## 💡 **进一步优化建议**

如果性能仍然不够理想，可以考虑以下方案：

### **方案1：数据库预计算（推荐）⭐⭐⭐⭐⭐**

**思路**：在数据入库时就计算并存储前复权价格

```sql
-- 1. 添加复权价格字段
ALTER TABLE stock_daily_data ADD COLUMN adj_open DECIMAL(10,3);
ALTER TABLE stock_daily_data ADD COLUMN adj_high DECIMAL(10,3);
ALTER TABLE stock_daily_data ADD COLUMN adj_low DECIMAL(10,3);
ALTER TABLE stock_daily_data ADD COLUMN adj_close DECIMAL(10,3);

-- 2. 创建定时任务预计算
CREATE OR REPLACE FUNCTION calculate_forward_adjusted_prices()
RETURNS void AS $$
BEGIN
    -- 批量计算所有股票的前复权价格
    -- ...
END;
$$ LANGUAGE plpgsql;

-- 3. 每日收盘后执行
SELECT calculate_forward_adjusted_prices();
```

**优点**：
- ✅ 查询时直接读取，无需计算
- ✅ 速度提升 **100倍以上**
- ✅ 首次过滤 < 5秒

**缺点**：
- ⚠️ 需要修改数据库结构
- ⚠️ 占用额外存储空间（约4倍）

---

### **方案2：Redis缓存复权K线数据 ⭐⭐⭐⭐**

**思路**：缓存计算好的复权K线，而不是KD值

```csharp
// 缓存复权后的K线数据
string cacheKey = $"adj_kline:{stockCode}";
var cachedKline = RedisHelper.GetCache<List<AdjustedKlineData>>(cacheKey);

if (cachedKline == null)
{
    // 计算并缓存
    cachedKline = CalculateAdjustedKline(stockCode);
    RedisHelper.SetCache(cacheKey, cachedKline, TimeSpan.FromDays(1));
}
```

**优点**：
- ✅ 二次访问极快（< 1秒）
- ✅ 不需要修改数据库

**缺点**：
- ⚠️ 首次仍需计算
- ⚠️ Redis内存占用增加

---

### **方案3：分批处理 + 懒加载 ⭐⭐⭐**

**思路**：不要一次性处理所有股票，按需加载

```csharp
// UI显示前100条
var topStocks = GetTopActiveStocks(100);  // 只处理活跃股票

// 用户滚动时再加载更多
foreach (var batch in allStocks.Batch(100))
{
    ProcessBatch(batch);
    UpdateUI();
}
```

**优点**：
- ✅ 首屏显示快（< 5秒）
- ✅ 用户体验好

**缺点**：
- ⚠️ 需要修改UI逻辑

---

### **方案4：增量计算 ⭐⭐**

**思路**：缓存历史复权价格，只计算新增数据

```csharp
// 检查缓存的最新日期
var cachedData = GetCachedAdjustedPrices(stockCode);
var lastCachedDate = cachedData.Keys.Max();

// 只计算新增的K线数据
var newData = dailyData.Where(d => d.TradeDate > lastCachedDate);
var adjNewData = CalculateAdjusted(newData);

// 合并
return cachedData.Concat(adjNewData);
```

**优点**：
- ✅ 减少重复计算

**缺点**：
- ⚠️ 复杂度高
- ⚠️ 除权事件变化时需要重新计算全部

---

## 🧪 **测试验证**

### **测试步骤**：

1. **点击"手动过滤"**
2. **观察控制台输出**：

```
[过滤开始] 总股票数: 5000, 目标日期: 2026-01-20
  进度: 500/5000 (10.0%), 已找到: 15 只
  进度: 1000/5000 (20.0%), 已找到: 32 只
  进度: 1500/5000 (30.0%), 已找到: 48 只
  进度: 2000/5000 (40.0%), 已找到: 65 只
  进度: 2500/5000 (50.0%), 已找到: 82 只
  进度: 3000/5000 (60.0%), 已找到: 98 只
  进度: 3500/5000 (70.0%), 已找到: 115 只
  进度: 4000/5000 (80.0%), 已找到: 132 只
  进度: 4500/5000 (90.0%), 已找到: 148 只
  进度: 5000/5000 (100.0%), 已找到: 165 只
[过滤完成] 处理: 5000只, 符合条件: 165只, 耗时: 25.3秒, 速度: 198只/秒
```

3. **评估性能**：
   - ✅ 首次过滤 < 40秒
   - ✅ 二次过滤 < 20秒
   - ✅ 后续刷新 < 10秒

---

## 🎉 **总结**

| 优化措施 | 效果 |
|---------|------|
| **算法优化** | ✅ 减少6.6倍计算量 |
| **并行度优化** | ✅ 减少线程竞争 |
| **进度显示** | ✅ 改善用户体验 |
| **综合提升** | **3-5倍性能提升** ⚡️ |

### **下一步建议**：

如果性能仍然不够理想，强烈建议采用 **数据库预计算方案**：
- ✅ 一次性投入，长期受益
- ✅ 性能提升 **100倍以上**
- ✅ 首次过滤 < 5秒

---

现在测试一下优化效果，看看能否达到预期的 **20-40秒** 完成首次过滤！🚀
