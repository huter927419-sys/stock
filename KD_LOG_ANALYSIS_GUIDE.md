# KD计算日志分析指南

## 快速开始

### 方法1：使用日志捕获脚本（推荐）

1. **运行日志捕获脚本**：
   ```cmd
   .\capture_kd_log.bat
   ```
   这会自动运行程序并将所有输出保存到日志文件。

2. **打开图表窗口**：
   - 运行程序后，打开任意股票的图表窗口
   - **特别推荐打开股票代码 `000001` 的图表**，因为会输出详细的KD调试信息

3. **停止程序**：
   - 按 `Ctrl+C` 停止程序
   - 日志文件会自动保存

4. **分析日志**：
   ```powershell
   .\analyze_kd_log.ps1 -LogFile "kd_log_20250127_143000.txt"
   ```

### 方法2：手动重定向输出

1. **运行程序并重定向输出**：
   ```cmd
   .\bin\Release\MQReceiver.exe > kd_log.txt 2>&1
   ```

2. **打开图表窗口**（特别是股票代码000001）

3. **停止程序**（Ctrl+C）

4. **分析日志**：
   ```powershell
   .\analyze_kd_log.ps1 -LogFile "kd_log.txt"
   ```

### 方法3：实时监控日志

```powershell
# 在一个终端运行程序
.\bin\Release\MQReceiver.exe > kd_log.txt 2>&1

# 在另一个终端实时监控
.\analyze_kd_log.ps1 -LogFile "kd_log.txt" -Watch
```

## 关键日志节点

### 1. KD调试信息（最重要）

```
[KD调试] 000001 week周期 (period=9):
  目标日期: 2025-01-27
  聚合后数据量: 150
  聚合数据首条: 2020-01-01
  聚合数据末条: 2025-01-27
  RSV计算完成，共 142 个值
    RSV[0]: 日期=2020-03-01, 收盘=10.50, 最高=11.20, 最低=9.80, RSV=45.50
    ...
  K和D值计算过程（前5个周期）:
    周期[0]: RSV=45.50, K=50.00 -> 48.50, D=50.00 -> 49.50
    ...
  最终结果: K=65.30, D=62.10, RSV=58.20, K-D=3.20
```

**检查点**：
- ✅ 聚合后数据量是否足够（应该 >= 9）
- ✅ RSV值是否合理（应该在0-100之间）
- ✅ K和D值是否合理（应该在0-100之间）
- ✅ K-D差值是否计算正确

### 2. 图表加载信息

```
[图表加载] 000001: KD计算完成 - 周KD=150, 月KD=50, 季KD=20
[图表加载] ⚠️ 警告: 000001 所有KD数据为空，可能原因：
  - 数据不足（需要至少9个周期的数据）
  - KD计算失败
  - 数据库连接问题
```

**检查点**：
- ✅ KD数据量是否 > 0
- ✅ 是否有警告信息
- ✅ 周/月/季KD数据量是否合理

### 3. WebChart调试信息

```
[WebChart调试] 股票: 000001
  日K线数据量: 1200
  周KD数据量: 150
  月KD数据量: 50
  季KD数据量: 20
  周KD数据样例:
    [0] Date=2020-01-06, K=45.30, D=48.20, K-D=-2.90
    ...
```

**检查点**：
- ✅ KD数据是否正确传递到前端
- ✅ K和D值是否合理
- ✅ K-D差值是否计算正确

### 4. KD计算信息

```
[KD计算] 000001 week周期: K线数据为空
[KD计算] 000001 week周期 2025-01-27 计算失败: [异常信息]
```

**检查点**：
- ⚠️ 是否有计算失败的信息
- ⚠️ 失败原因是什么

## 常见问题诊断

### 问题1：KD数据为空

**症状**：
```
[图表加载] ⚠️ 警告: 000001 所有KD数据为空
```

**可能原因**：
1. **数据不足**：聚合后的数据量 < 9
   - 检查：查看 `[KD调试]` 中的"聚合后数据量"
   - 解决：确保从股票的最早数据开始计算

2. **KD计算失败**：
   - 检查：查看 `[KD计算]` 中的错误信息
   - 解决：检查数据库连接和数据质量

3. **数据范围问题**：
   - 检查：查看 `[KD调试]` 中的"目标日期"和"聚合数据末条"
   - 解决：确保目标日期在数据范围内

### 问题2：KD值不合理

**症状**：
```
最终结果: K=150.30, D=200.10  # K和D值超出0-100范围
```

**可能原因**：
1. **计算公式错误**（已检查，公式正确）
2. **数据质量问题**：价格数据异常
   - 检查：查看 `[KD调试]` 中的RSV计算详情
   - 解决：检查原始K线数据

### 问题3：K-D差值计算错误

**症状**：
- 图表显示K-D差值，但值不合理
- 或者图表没有显示K-D差值

**可能原因**：
1. **前端计算问题**：
   - 检查：查看浏览器控制台（F12）的 `calculateKDDiff` 输出
   - 解决：检查前端代码

2. **数据传递问题**：
   - 检查：查看 `[WebChart调试]` 中的KD数据量
   - 解决：确保K和D值都正确传递

### 问题4：图表没有显示

**症状**：
- KD值计算正确，但图表没有显示

**可能原因**：
1. **前端问题**：
   - 检查：查看浏览器控制台（F12）的错误信息
   - 解决：检查 `setKDBandData` 和 `calculateKDDiff` 的输出

2. **图表初始化问题**：
   - 检查：查看浏览器控制台的 `weeklyBandSeries` 初始化信息
   - 解决：确保图表系列正确初始化

## 日志分析脚本功能

`analyze_kd_log.ps1` 脚本会自动分析以下内容：

1. **KD调试信息**：按股票代码和周期分组显示
2. **图表加载信息**：显示最近的图表加载日志
3. **KD计算信息**：统计成功和失败次数
4. **WebChart调试信息**：显示前端数据传递情况
5. **错误和异常**：列出所有错误信息
6. **KD数据统计**：显示KD数据量统计
7. **问题诊断**：自动诊断可能的问题

## 提供日志的方法

### 方法1：直接提供日志文件
1. 运行 `capture_kd_log.bat` 捕获日志
2. 打开图表窗口（特别是股票代码000001）
3. 停止程序
4. 将日志文件发送给我

### 方法2：复制控制台输出
1. 运行程序
2. 打开图表窗口
3. 复制控制台的所有输出
4. 粘贴到文本文件
5. 发送给我

### 方法3：使用分析脚本
1. 运行 `analyze_kd_log.ps1` 分析日志
2. 将分析结果发送给我

## 需要提供的信息

分析日志时，请提供：
1. **完整的日志文件**（或分析结果）
2. **打开的股票代码**（特别是是否打开了000001）
3. **图表显示情况**（是否有显示，显示什么）
4. **浏览器控制台输出**（如果有）

---

**请运行程序并打开图表，然后提供日志文件，我会帮您分析问题！**
