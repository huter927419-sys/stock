# 复权价格实时计算实现说明

## 修改内容

修改了 `PostgresKlineDataRepository.cs` 的以下两个方法：
- `GetDailyData()` - 获取指定时间范围的日线数据
- `GetLatestDailyData()` - 获取最新N条日线数据

## 实现原理

### 1. 不依赖预计算字段
之前的方案需要预先填充 `adjusted_open_price`, `adjusted_high_price` 等字段。

**新方案**：直接在SQL查询中，从 `stock_exrights_data` 表实时计算复权价格。

### 2. 前复权计算公式

对于每个交易日，计算该日期**之后**所有除权事件的累积影响：

#### 送股调整
- **调整因子**：`1 / (1 + 送股比例)`
- **示例**：10送3股，因子 = 1 / (1 + 0.3) = 0.769

#### 配股调整（简化版）
- **调整因子**：`1 + 配股比例`
- **注**：完整的配股计算需要除权前一天的收盘价，公式为：
  ```
  (价格 + 配股价 * 配股比例) / (1 + 配股比例)
  ```
  当前实现为简化版本，后续可优化。

#### 分红调整
- **调整方式**：价格 - 累积分红
- **示例**：如果之后有2次分红，每股0.5元，则当前价格需减去1元

#### 综合计算
```
复权价格 = (原始价格 - 累积分红) × 累积送配股因子
```

### 3. SQL实现（CTE分步查询）

```sql
WITH exrights_factors AS (
    -- 步骤1：计算每个除权事件的调整因子
    SELECT stock_code, ex_rights_date,
           give_factor, pei_factor, profit_per_share
    FROM stock_exrights_data
),
daily_with_adjustment AS (
    -- 步骤2：为每个交易日计算累积调整因子
    SELECT d.trade_date, d.open_price, d.high_price, ...,
           -- 计算该日期之后所有除权的累积因子
           EXP(SUM(LN(give_factor * pei_factor))) as cumulative_factor,
           -- 计算该日期之后所有分红的累积值
           SUM(profit_per_share) as cumulative_dividend
    FROM stock_daily_data d
)
SELECT
    trade_date,
    -- 步骤3：应用复权调整
    (open_price - cumulative_dividend) * cumulative_factor as adjusted_open,
    (high_price - cumulative_dividend) * cumulative_factor as adjusted_high,
    (low_price - cumulative_dividend) * cumulative_factor as adjusted_low,
    (close_price - cumulative_dividend) * cumulative_factor as adjusted_close,
    volume
FROM daily_with_adjustment
```

### 4. 优点

✅ **无需预计算**：不需要提前填充 `adjusted_*_price` 字段
✅ **实时更新**：除权数据表一旦有新数据，立即生效
✅ **降低存储**：不需要额外存储复权价格字段
✅ **灵活切换**：可以轻松切换前复权/后复权/不复权

### 5. 缺点

⚠️ **性能开销**：每次查询都需要计算，相比直接读取字段会慢一些
⚠️ **配股计算简化**：当前配股调整未使用除权前收盘价（完整公式较复杂）

### 6. 性能优化建议

如果查询性能成为瓶颈，可考虑：
1. 添加索引：`CREATE INDEX idx_exrights_code_date ON stock_exrights_data(stock_code, ex_rights_date DESC)`
2. 使用Redis缓存：缓存计算结果
3. 定期预计算：夜间批量计算并存储到 `adjusted_*_price` 字段

## 使用示例

### 示例1：没有除权数据的股票
- 从 `stock_exrights_data` 查不到数据
- `cumulative_factor` = 1.0
- `cumulative_dividend` = 0.0
- **结果**：复权价格 = 原始价格（无调整）

### 示例2：有除权数据的股票

假设股票000001有以下除权记录：
- 2024-05-20：10送3股，派0.5元
- 2024-08-15：10配2股@5元，派0.3元

查询2024-01-01的K线数据：
1. 该日期之后有2次除权
2. 送股因子：1/(1+0.3) = 0.769
3. 配股因子：1+0.2 = 1.2（简化）
4. 累积因子：0.769 × 1.2 = 0.923
5. 累积分红：0.5 + 0.3 = 0.8
6. 如果原始收盘价10元：
   - 复权收盘价 = (10 - 0.8) × 0.923 = 8.49元

## 验证方法

### 方法1：对比原始价格和复权价格
运行过滤器，查看KD值是否有变化（复权后K线应该更平滑）

### 方法2：查看除权日前后的价格
在有除权的股票上，查看除权日前后的K线，复权后应该连续，原始价格会有跳空。

### 方法3：SQL直接验证
```sql
-- 查看某股票的原始价格和复权价格对比
SELECT
    trade_date,
    close_price as 原始收盘价,
    (close_price - (
        SELECT COALESCE(SUM(profit_per_share), 0)
        FROM stock_exrights_data
        WHERE stock_code = '000001'
        AND ex_rights_date > d.trade_date
    )) as 减去分红后,
    -- ... 完整计算
FROM stock_daily_data d
WHERE stock_code = '000001'
ORDER BY trade_date DESC
LIMIT 10;
```

## 下一步工作

1. ✅ 已完成：实时计算前复权价格
2. ⏳ 待优化：完善配股调整公式（需要除权前收盘价）
3. ⏳ 待验证：测试有除权数据的股票，确认KD计算正确
4. ⏳ 待优化：如果性能有问题，添加索引或缓存

## 注意事项

⚠️ **如果 `stock_exrights_data` 表为空**：
- 查询仍然可以正常执行
- 返回的是原始价格（因为没有除权调整）
- 不会报错

⚠️ **配股计算的局限**：
- 当前配股因子使用简化公式
- 完整公式需要：`(价格 + 配股价 * 配股比例) / (1 + 配股比例)`
- 需要除权前一天的收盘价，SQL会更复杂

⚠️ **性能考虑**：
- 对于频繁查询的场景，建议添加缓存
- 如果除权数据很多（>100条/股票），可能需要优化
